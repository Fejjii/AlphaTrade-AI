Status: IN_PROGRESS
Last Updated: 2026-09-22T14:44:28+00:00
Task: AT-077 — Controlled Telegram paper activation
Current Phase: CI confirmation
Progress: Implementation committed on cursor/activation_telegram_paper-a361 at 4f244d6. Draft PR #129 is open. Local validation supports READY FOR CONTROLLED ACTIVATION. GitHub CI run 35742331530 is still in progress.
Blocker: None
Human Action Needed: None
Next Step: Confirm CI run 35742331530, then set this handoff to READY if it is green. Do not deploy or arm Telegram.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-22T14:44:28+00:00
Generated At UTC: 2026-09-22T14:44:28Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260922-144428
Task ID: AT-077
Current Branch: cursor/activation_telegram_paper-a361
Current Commit: 4f244d6
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: IN_PROGRESS
Source File SHA256: 679da7ba05ab8845c72b98bdf8cdc40b4834784c662c477210614a7a43b717a1
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Paper Telegram activation machinery is implemented and stays disarmed. Local tests support **READY FOR CONTROLLED ACTIVATION**. GitHub CI run 35742331530 was still running when this snapshot was written, so the handoff status remains `IN_PROGRESS` until that run finishes.
2. **Goal** — Complete the production-shaped path Watcher/Candidate event → durable outbox → Telegram alert → identity-gated paper discussion, with preflight, smoke, and rollback. Do not activate Watcher, enable live trading, change staging environment variables, send a real Telegram message, or deploy.
3. **Current status** — `IN_PROGRESS` only because CI has not returned. The process default verdict is `NOT_ARMED`. `render.yaml` is unchanged.
4. **Current branch, commit, and phase** — `cursor/activation_telegram_paper-a361` at `4f244d6`, based on `main` `894e9e4`. Draft PR https://github.com/Fejjii/AlphaTrade-AI/pull/129. Phase: CI confirmation.
5. **Last completed task** — AT-077 implementation, local validation, and draft PR. Activation itself was not performed.
6. **Architecture summary** — `app.telegram_activation` is the paper controller. `arm()` fails closed unless preflight passes and does not flip settings. `create_app` does not mount `/webhooks/telegram/paper` and does not poll. Durable pieces are the existing security outbox, Alembic `e0f1a2b3c4d5` cursor and send ledger, backoff from `attempt` + `updated_at` (5s, 30s, 120s), and protocol rate-limit defers that do not burn an attempt. Staging and production reject armed, non-off inbound mode, network permit, and a non-empty webhook secret.
7. **Files changed** — `backend/src/app/telegram_activation/`, `backend/src/app/telegram_security/backoff.py`, protocol/store claim backoff, Postgres cursor and ledger, health posture, deployment safety, example env files, `docker-compose.yml`, scripts `telegram-paper-activation-preflight.sh`, `telegram-paper-activation-smoke.sh`, `telegram-paper-activation-rollback.sh`, `docs/telegram_paper_activation.md`. `render.yaml` was not edited.
8. **Important code references** — `TelegramPaperActivation.arm`, `run_preflight`, `deliver_due`, `GuardedTelegramTransport`, `mount_paper_telegram_webhook`, `GET /health/telegram-paper-activation`.
9. **Tests and validation** — `uv run ruff check .` and `uv run ruff format --check .` passed. Full `uv run pytest -q --tb=line` failed only `test_blackbox_telegram_cannot_place_orders` and `test_no_inbound_telegram_route_exists` because the new read-only health path was outside the known route set. Those allowlists were updated. Re-run of those two tests plus `tests/test_telegram_paper_activation.py`, `tests/test_telegram_paper_activation_postgres.py`, and `tests/test_phase2_4_alembic_postgres.py` passed (20). Frontend lint, typecheck, 1195 unit tests, and build passed. Evaluation 16/16, RAG 5/5, guardrails 7/7. Preflight `--expect-disabled` exit 0 (`NOT_ARMED`, `runtime_armable` false). Rollback `--apply` exit 2. Docker image build was not run locally (no Docker daemon). CI run 35742331530 was in progress.
10. **Deployment and provider status** — Not deployed. Not activated. No live Telegram HTTP call. Provider mode was not changed.
11. **Security and trading safety status** — `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`. Watcher orchestration stays off. Telegram flags stay false and inbound mode stays `off`. Paper mutations remain confirmation-gated. Telegram cannot mint a Candidate, override SetupAssessment or risk, activate a strategy, create a live order, or enable live trading. `APPROVE` stays `executes=false`.
12. **Decisions confirmed** — AT-ADR-056. Rollback is a printed checklist. The rollback command refuses `--apply`.
13. **Known issues and blockers** — None in the implementation. CI result is not yet recorded. Local outbound rate-limit window is in memory; the durable wait for `RATE_LIMITED` is the outbox `updated_at` backoff, covered by the PostgreSQL test.
14. **Remaining prioritized tasks** — Wait for CI. Do not arm, deploy, or change staging variables. A later human activation is a separate local paper action after preflight.
15. **Recommended model** — Cursor Grok 4.6 Extra High
16. **Exact next instruction for ChatGPT** — Read this handoff. If CI run 35742331530 is green, the task verdict is READY FOR CONTROLLED ACTIVATION and the process must stay `NOT_ARMED`. If CI failed, do not arm; record the failed check.
17. **Exact next Cursor prompt** — Confirm GitHub CI run 35742331530 for `cursor/activation_telegram_paper-a361`. If it is green, set HANDOFF status to READY and keep the verdict READY FOR CONTROLLED ACTIVATION without deploying or arming. If it failed, fix only the failure and do not activate Telegram.
