Status: READY
Last Updated: 2026-09-22T15:02:42+00:00
Task: AT-077 — Controlled staging read-only Binance USD-M evidence
Current Phase: Complete
Progress: Activation profile, health visibility, validator, and CI evidence are on the draft PR. Live staging env was not changed.
Blocker: None
Human Action Needed: None
Next Step: When a human chooses to activate, apply docs/live_market_staging_activation.md on both staging services and restart. Do not enable Watcher, Telegram, or live trading.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-22T15:02:42+00:00
Generated At UTC: 2026-09-22T15:02:42Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260922-150242
Task ID: AT-077
Current Branch: cursor/activation_live_market
Current Commit: fe705a2
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: READY
Source File SHA256: 26a8776510216769069508c93c039d4b42a3a4375ee9334f7accf23f994392f4
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Staging can be switched to real read-only Binance USD-M evidence through `PERPETUAL_EVIDENCE_SOURCE=binance_usdm`. Replay remains the process default and the rollback value. Watcher, Telegram, and live trading stay off. No credentials. No deploy. Verdict: READY FOR CONTROLLED ACTIVATION.
2. **Goal** — Replace replay as the intended staging evidence source with the existing public `binance_usdm` provider, fail closed on unsafe profiles, and document activation and rollback.
3. **Current status** — READY. Draft PR https://github.com/Fejjii/AlphaTrade-AI/pull/128. Implementation `ed17355` passed GitHub CI run 35740379070. Branch HEAD `fe705a2` records that evidence in `.ai/TASKS.md` and does not change runtime behavior. The follow-up CI run for `fe705a2` was not waited on.
4. **Current branch, commit, and phase** — Branch `cursor/activation_live_market`. HEAD `fe705a2912d1236e4215fabf117c98ecde4dfc28`. Base `main@894e9e4`. Phase complete.
5. **Last completed task** — AT-077. ADR AT-ADR-056.
6. **Architecture summary** — `market_activation.profile` accepts `binance_usdm` only for a public `https://fapi.binance.com` origin, paper internal execution, no Binance or BloFin credentials, and Watcher/Telegram flags false. Production refuses the live source. `GET /health` and `GET /canonical/market-status` report activation. Freshness stays 10 seconds. BTCUSDT stays the default catalog symbol. Spot and fabricated fallback stay forbidden.
7. **Files changed** — Activation module `backend/src/app/market_activation/`, Settings validation, health and market-status schemas, `.env.staging.example`, `render.yaml`, `scripts/validate-live-market-staging.sh`, `docs/live_market_staging_activation.md`, tests, and CI self-check. 33 files, +1379 / −24 versus `main`.
8. **Important code references** — `backend/src/app/market_activation/profile.py`; `docs/live_market_staging_activation.md`; `scripts/validate-live-market-staging.sh`.
9. **Tests and validation** — GitHub CI run https://github.com/Fejjii/AlphaTrade-AI/actions/runs/35740379070 on `ed17355` succeeded. Backend `uv run pytest`: 2549 passed, 236 warnings, 1693.63s. Ruff check passed. Deployment-safety pytest showed 60 tests at 100% and `./scripts/validate-live-market-staging.sh --self-check` passed. Frontend 1196 passed and the build succeeded. Evaluation 16/16, 5/5, 7/7. E2E 30 passed / 13 skipped. Docker image `alphatrade-backend:ci` built. Local activation suite 17 passed before CI. Local full pytest exited 0 with Postgres down, so the local pass/skip split was not retained. The market-status card label was covered by vitest. The staging UI was not opened in a browser because this run did not deploy.
10. **Deployment and provider status** — Not deployed. Live staging environment variables were not modified. Intended staging source after a human applies the runbook: `binance_usdm` at `https://fapi.binance.com`. Process default and rollback: `replay`. `MARKET_DATA_PROVIDER=binance` is not the evidence source and is not a fallback.
11. **Security and trading safety status** — `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`, `EXCHANGE_MODE=paper_internal`. No exchange credentials. Public GET only. No authenticated mutation. No spot fallback. No fabricated fallback. Watcher and Telegram remain disabled.
12. **Decisions confirmed** — AT-ADR-056. Staging intended source is `binance_usdm`. Code default and rollback stay `replay`. Production cannot select the live source.
13. **Known issues and blockers** — None for this preparation. The Mac handoff sync script is not on this Linux host; the GitHub handoff branch is the cloud publish path.
14. **Remaining prioritized tasks** — Human activation is optional and separate: set the documented env on the staging API and worker, restart, and run the remote validator. Do not merge or deploy until that human intends to activate.
15. **Recommended model** — Cursor Grok 4.6 Extra High
16. **Exact next instruction for ChatGPT** — Treat AT-077 as complete and READY FOR CONTROLLED ACTIVATION. Do not deploy, do not edit the live staging environment, and do not enable Watcher, Telegram, or live trading. Activation and rollback steps are in `docs/live_market_staging_activation.md`.
17. **Exact next Cursor prompt** — Do not run unless a human has already set the staging env and restarted both services. Then run `BASE_URL=https://YOUR-API.onrender.com ./scripts/validate-live-market-staging.sh --remote --require-active` and report health source, activation, freshness, and whether BTCUSDT market status is `binance_usdm_futures_public` with `fallback_used=false`. Do not change env vars, deploy, or enable Watcher, Telegram, or live trading.

## Exact staging environment changes

Apply on the staging API and worker together. Do not set `BINANCE_*` or `FAPI_*` keys.

| Variable | Activation value |
|---|---|
| `PERPETUAL_EVIDENCE_SOURCE` | `binance_usdm` |
| `MARKET_DATA_FUTURES_BASE_URL` | `https://fapi.binance.com` |
| `PERPETUAL_EVIDENCE_TIMEOUT_SECONDS` | `10` |

Leave `ENVIRONMENT=staging`, `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`, `EXCHANGE_MODE=paper_internal`, BloFin demo and credentials unset, and every Watcher and Telegram flag false.

## Rollback

Set `PERPETUAL_EVIDENCE_SOURCE=replay` on both services, restart, and run `./scripts/validate-live-market-staging.sh --remote --expect inactive`. Health activation becomes `inactive`. Market status presentation is `replay_fixture`.
