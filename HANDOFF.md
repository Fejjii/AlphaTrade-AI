Status: REVIEW_REQUIRED
Last Updated: 2026-09-25T08:10:07+00:00
Task: Live BTCUSDT perpetual evidence without exclusive Binance Render IP dependence
Current Phase: Implementation complete — draft PR open, not deployed
Progress: OKX USDT-swap secondary source implemented and targeted tests passed
Blocker: None
Human Action Needed: Review draft PR #140, then apply the staging env block. Do not arm Watcher.
Next Step: Human review of PR #140 before any staging env change

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-25T08:10:07+00:00
Generated At UTC: 2026-09-25T08:10:07Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260925-081007
Task ID: LIVE-PERP-EVIDENCE-OKX-FAILOVER
Current Branch: cursor/okx-perp-evidence-failover-98bd
Current Commit: d8d5722
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: 9d30cba9b98723d6f91278fe5eb2b9d3985ce2edea1cd0852a7801646057b189
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Paper staging can keep Binance USD-M as the primary public perpetual source and fail over the whole BTCUSDT observation to OKX linear `BTC-USDT-SWAP` when Binance returns HTTP 418, HTTP 429, or is unreachable. Provenance stays on the venue that produced the row. Draft PR #140. No deploy. Watcher and Telegram stay disarmed. Real trading stays off.

2. **Goal** — A stable live BTCUSDT perpetual evidence path that does not depend only on the Binance Render IP, still matching the canonical evidence contract.

3. **Current status** — `REVIEW_REQUIRED`. Implementation and targeted validation are done. A human must review the draft PR and apply staging configuration. This run did not deploy.

4. **Current branch, commit, and phase** — `cursor/okx-perp-evidence-failover-98bd` at `d8d5722`. Phase: implementation complete, review pending. PR: https://github.com/Fejjii/AlphaTrade-AI/pull/140

5. **Last completed task** — OKX public swap adapter, primary/secondary failover, watcher source-family match, staging blueprint values, and targeted tests.

6. **Architecture summary** — `PERPETUAL_EVIDENCE_SOURCE=binance_usdm` with `PERPETUAL_EVIDENCE_SECONDARY_SOURCE=okx_usdt_swap` builds `FailoverPerpetualSource`. A primary ban, rate limit, or regional failure raises `EvidenceSourceSwitchRequiredError` and does not return the Binance payload. The monitor rebinds to `okx:usdm_futures:perpetual:BTCUSDT`, starts a new connection epoch, and reads `https://www.okx.com` GET `/api/v5/market/candles` and `/api/v5/market/history-trades` only. Contract multiplier is 0.01 BTC. Taker side `buy`/`sell` drives CVD. History that does not reach the requested start fails closed. Spot instId `BTC-USDT` is rejected. Recovery calls Binance `status()` and switches back only when that report is healthy, on a new epoch. `semantic_source_from_identity` no longer relabels OKX as Binance.

7. **Files changed** — `backend/src/app/market_contracts/adapters/okx_usdt_swap.py`, `failover.py`, factory, http allowlist parameter, catalog, identity, enums, first-slice identity, ohlcv family allowlist, monitor runtime rebind, evidence assembler/service, watcher reconcile, fusion market identity, activation profile, health schema, `render.yaml`, `.env.staging.example`, `docs/live_market_staging_activation.md`, `.ai/DECISIONS.md` AT-ADR-067, `backend/tests/test_okx_usdt_swap_evidence.py`.

8. **Important code references** — `OkxUsdtSwapPerpetualSource`, `FailoverPerpetualSource`, `resolve_perpetual_evidence_source`, `SymbolMonitorRuntime._rebind`, `instrument_for_source`.

9. **Tests and validation** — `uv run pytest tests/test_okx_usdt_swap_evidence.py tests/test_watcher_source_gate.py tests/test_deployment_safety.py tests/test_health.py tests/test_execution_credential_isolation.py tests/test_live_market_activation.py tests/test_binance_usdm_staging_reliability.py` passed (the combined runs were exit 0; the OKX file is 9 passed). `uv run ruff check` on the touched modules passed. `uv run mypy` on the failover, factory, evidence service, and monitor runtime passed with no errors. Full-suite pytest was not run. Docker deploy was not run.

10. **Deployment and provider status** — Not deployed. Blueprint now records secondary `okx_usdt_swap` and `OKX_SWAP_BASE_URL=https://www.okx.com` beside primary `binance_usdm`. Watcher service in `render.yaml` stays disarmed.

11. **Security and trading safety status** — `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`, `EXCHANGE_MODE=paper_internal`. No Binance credential use. No order, position, or account paths. Spot and fabricated fallbacks remain false. Live trading was not enabled.

12. **Decisions confirmed** — AT-ADR-067. OKX is a separate venue, not a Binance price substitute. Historical windows that cannot be proved fail closed rather than inventing prints. An 8-hour first-slice CVD backfill may fail closed on OKX page caps (default 8 pages); the live freshness window and forward monitor stream are the covered path.

13. **Known issues and blockers** — None blocking the review. OKX public history is paged at 100 rows and will not silently fill an 8-hour gap. Fusion accepts an OKX BTCUSDT linear swap identity; it still rejects spot, fallback, and a Binance/OKX mix.

14. **Remaining prioritized tasks** — Human review and merge of PR #140. Human applies the staging env. Do not arm Watcher or Telegram in that step.

15. **Recommended model** — Any current Cursor default model.

16. **Exact next instruction for ChatGPT** — Review draft PR #140. Confirm OKX rows are never labelled Binance, spot is rejected, and Watcher stays off. After merge, a human may set the staging env block. Do not deploy or arm from chat.

17. **Exact next Cursor prompt** —

```
TARGET: STAGING ENV ONLY — do not deploy from the agent unless a human already applied it.
After PR #140 is merged, confirm staging API and disarmed watcher have:
PERPETUAL_EVIDENCE_SOURCE=binance_usdm
PERPETUAL_EVIDENCE_SECONDARY_SOURCE=okx_usdt_swap
MARKET_DATA_FUTURES_BASE_URL=https://fapi.binance.com
OKX_SWAP_BASE_URL=https://www.okx.com
EXECUTION_MODE=paper
ENABLE_REAL_TRADING=false
EXCHANGE_MODE=paper_internal
Watcher and Telegram flags false.
Read GET /health and confirm perpetual_evidence_secondary_source=okx_usdt_swap,
spot_fallback_permitted=false, fabricated_fallback_permitted=false.
Do not arm Watcher.
```

- exact blocker: None. Human review is required before merge or staging env edits.
- exact command or action that failed: None.
- concise relevant error output: None.
- what was already completed: OKX secondary adapter, failover, tests, draft PR #140 at `d8d5722`.
- what remains unchanged: Watcher disarmed, Telegram disarmed, real trading disabled, no deploy.
- risk of continuing: Applying the env or merging without review would change staging evidence before approval.
- whether application code was modified: Yes, on `cursor/okx-perp-evidence-failover-98bd`.
- whether uncommitted work exists: No product changes uncommitted. This handoff is published on the handoff branch only.
- exact human action required: Review https://github.com/Fejjii/AlphaTrade-AI/pull/140 and apply the staging env only after approval.
- exact Cursor instruction after resolution: Use the prompt in section 17.
