Status: REVIEW_REQUIRED
Last Updated: 2026-09-21T15:48:00+00:00
Task: AT-069 — Paper-only continuous Watcher runtime
Current Phase: CI follow-up on draft PR #120 — concurrent-lease test isolated from SQLite
Progress: Feature on `f32daec`; 22/22 paper-runtime tests; local full pytest 2216 passed / 181 skipped; GitHub CI on this head in progress
Blocker: None
Human Action Needed: Do not merge. Do not deploy. Wait for GitHub CI green on `f32daec`, then independent review of draft PR #120
Next Step: If GitHub CI is green, stop. If it fails, fix remaining failures on `cursor/watcher_paper_runtime-5455`

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-21T15:48:00+00:00
Generated At UTC: 2026-09-21T15:48:00Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260921-154800
Task ID: AT-069
Current Branch: cursor/watcher_paper_runtime-5455
Current Commit: f32daec
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: dbc7fcd8f3b20b0bee983ecb69e89b4648d9accfb0109f623990aa325e0cb85a
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — AT-069 paper-only continuous Watcher runtime is implemented on
   `cursor/watcher_paper_runtime-5455` @ `f32daec` (base `main@b4244f0`). Canonical chain:
   approved compiled strategy → live read-only evidence → `WatcherOrchestrator` scan →
   `evaluate_canonical_strategy` → Candidate only on `CONFIRMED_SETUP`. Draft PR
   https://github.com/Fejjii/AlphaTrade-AI/pull/120. GitHub backend CI on `1de8b60`
   (run 35614998968) failed `test_concurrent_workers_single_lease` because two workers
   shared StaticPool SQLite; follow-up `f32daec` isolates that test and surfaces worker
   exceptions. Local pytest on `f32daec` is green. Do not merge. Do not deploy.

2. **Goal** — Wire the missing continuous paper Watcher worker without enabling
   staging/production flags, Telegram, live orders, real exchange credentials, or a second
   evaluator.

3. **Current status** — `REVIEW_REQUIRED`. Implementation complete; CI flake fix pushed;
   draft PR open; GitHub CI on `f32daec` not yet confirmed green at handoff time
   (run 35619201234 in_progress).

4. **Current branch, commit, and phase** — Product branch
   `cursor/watcher_paper_runtime-5455` @ `f32daec2be423dc500ee1446fe091f8a7ff24ea0`.
   Commits: `1de8b60` feat runtime; `f32daec` concurrent-test CI fix. Phase: review /
   wait for exact-head CI.

5. **Last completed task** — Isolated concurrent lease test from SQLite; re-ran paper
   runtime tests (22 passed, concurrent 5/5 repeats); full local backend pytest; evaluation
   and deployment-safety self-checks.

6. **Architecture summary** — Dedicated process `python -m app.workers.watcher_paper` is
   the canonical worker. API autostart only when `paper_runtime_enabled`: local + paper +
   `WATCHER_ORCHESTRATION_ENABLED` + real trading false. Staging/production still reject
   the flag. Targets are tenant-scoped APPROVED/ACTIVE compiled strategies via
   `resolve_executable_strategy_policy`; BTCUSDT is always first. Scan unit is
   tenant × compiled version × symbol. Leases/fencing from `WatcherOrchestrator` +
   `PostgresWatcherStore` (tests use `InMemoryWatcherStore`). Evidence:
   `AssemblingWatcherScanEvidence` fail-closed on stale/outage. Evaluation:
   `WatcherFusionEvaluationService` → `evaluate_canonical_strategy`. Candidate persist
   requires `ExecutablePolicyAuthority.PERSISTED_APPROVED_COMPILED` and evidence-time
   `BoundEvaluationClock`. Kill switch does not block monitoring or Candidate persist and
   must not invoke execution or Telegram. Status: `GET /watcher/paper-runtime/status`.
   Metrics: `watcher_paper_*` on the existing Prometheus registry.

7. **Files changed** — vs `main@b4244f0` (17 files, +2062/-14): workers
   `watcher_paper.py` / `watcher_paper_targets.py`; `paper_metrics.py`; schemas/routes
   `watcher_paper`; config symbols/poll/max_scopes/worker_id; main lifespan autostart;
   evidence port fail-closed mapping; 22 tests in `test_watcher_paper_runtime.py`;
   AT-ADR-048 / AT-069 TASKS / ARCHITECTURE. Follow-up `f32daec` touches only the
   concurrent test and TASKS completion evidence.

8. **Important code references** —
   - `backend/src/app/workers/watcher_paper.py` (`WatcherPaperRuntime`,
     `paper_runtime_enabled`, `build_watcher_paper_runtime`)
   - `backend/src/app/workers/watcher_paper_targets.py` (`list_paper_scan_targets`,
     `normalize_paper_symbols`)
   - `backend/src/app/api/routes/watcher_paper.py` (`GET /watcher/paper-runtime/status`)
   - `backend/src/app/schemas/watcher_paper.py`
     (`WATCHER_PAPER_ACTIVATION_REQUIREMENTS`)
   - existing `WatcherOrchestrator`, `PostgresWatcherStore`,
     `AssemblingWatcherScanEvidence`, `resolve_executable_strategy_policy`,
     `CandidateLifecycleService`

9. **Tests and validation** —
   - Targeted: `uv run pytest tests/test_watcher_paper_runtime.py` → 22 passed, exit 0.
     Concurrent test repeated 5/5, exit 0.
   - Ruff: `uv run ruff check .` pass; `uv run ruff format --check .` 806 files already
     formatted.
   - Full backend: `uv run pytest` → 2216 passed, 181 skipped, 237 warnings, exit 0
     (17:27). Postgres-backed tests skipped locally (no local Postgres); GitHub CI has
     the service. Prior CI on `1de8b60` was 2396 passed + 1 failed (the concurrent test).
   - Deployment safety: `uv run pytest tests/test_deployment_safety.py
     tests/test_deployment_scripts.py tests/test_config.py -q` → 60 passed, exit 0.
     `scripts/post-deploy-smoke-gate.sh --self-check` pass;
     `scripts/canonical-staging-smoke.sh --self-check` pass.
   - Evaluation: agent 16/16, RAG 5/5, guardrails 7/7.
   - Frontend / e2e / docker-build: not re-run locally this follow-up (no frontend
     change). Prior product commit `1de8b60` GitHub jobs frontend, deployment-safety,
     docker-build succeeded; evaluation/e2e skipped because backend failed.
   - GitHub CI on `f32daec`: UNKNOWN at this handoff (run 35619201234 in_progress).

10. **Deployment and provider status** — No deploy. No staging/production flag enabled.
    `.env.example` still `WATCHER_ORCHESTRATION_ENABLED=false`. `render.yaml` untouched.

11. **Security and trading safety status** — Paper monitoring only. `EXECUTION_MODE=paper`,
    `ENABLE_REAL_TRADING=false`. No live orders, no Telegram, no real exchange
    credentials. Candidate persist still requires persisted approved compiled authority.
    Kill switch continues evaluation and must not invoke execution/Telegram (asserted).

12. **Decisions confirmed** — AT-ADR-048. Dedicated process is the canonical worker.
    In-process API autostart is local-paper only. No second evaluator.

13. **Known issues and blockers** —
    - Exact blocker: none technical; review gate is GitHub CI on `f32daec` plus
      independent review. Do not merge.
    - Exact prior failure: `uv run pytest` on GitHub run 35614998968 /
      `test_concurrent_workers_single_lease` —
      `AssertionError: assert ('skipped' in ['succeeded'] or 'lease_held' in {'confirmed_setup'})`.
      One worker died on shared StaticPool SQLite before lease contention.
    - Completed: runtime + tests + concurrent isolation fix.
    - Unchanged: strategy authority, Telegram, live trading, staging/production flags.
    - Risk of continuing: merging before exact-head CI green could land a flaky test.
    - Application code this follow-up: not modified (tests + TASKS only).
    - Uncommitted work on product branch: none.

14. **Remaining prioritized tasks** — Keep Watcher flags false in staging/production;
    local paper activation only after review; no merge/deploy of this PR by this agent.

15. **Recommended model** — Cursor Grok 4.6 Extra High

16. **Exact next instruction for ChatGPT** — Treat draft PR #120 @ `f32daec` as the
    AT-069 implementation. Do not plan merge or deploy. Confirm GitHub CI green on that
    exact head. Remaining activation is local paper only and is listed below.

17. **Exact next Cursor prompt** —

```text
If GitHub CI on cursor/watcher_paper_runtime-5455 @ f32daec is green, stop.
Do not merge. Do not deploy. Do not enable staging/production Watcher flags.
If CI failed, fix only the remaining failures on that branch, keep paper-only
invariants, update draft PR #120, and regenerate HANDOFF.
```

## Remaining activation requirements (not done by this PR)

1. Keep `WATCHER_ORCHESTRATION_ENABLED=false` in staging and production
2. Local only: `ENVIRONMENT=local`, `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`
3. At least one tenant-scoped APPROVED or ACTIVE compiled strategy
4. Read-only perpetual evidence (`replay` default, or `binance_usdm`)
5. Dedicated paper Watcher process or local autostart (`python -m app.workers.watcher_paper`)
6. Telegram stays disabled; Watcher never places orders
7. No real exchange credentials
