Status: REVIEW_REQUIRED
Last Updated: 2026-08-06T13:18:00+00:00
Task: P0 — Paper close exit price honesty (fix/paper_close_exit_price_honesty)
Current Phase: IMPLEMENTATION COMPLETE — awaiting human PR review/merge decision
Progress: Root cause proven; fix implemented; 21 new regression tests; full backend suite 1394 passed / 11 skipped; frontend lint+typecheck+1137 tests+build pass; e2e chromium smoke 20 passed
Blocker: None
Human Action Needed: Review and merge draft PR for branch fix/paper_close_exit_price_honesty; after merge + staging redeploy, re-run the staging exit-price drill
Next Step: Human review of draft PR; then staging redeploy and exit-price/journal/portfolio browser gate re-run

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-08-06T13:18:00+00:00
Generated At UTC: 2026-08-06T13:18:00Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260806-131800
Task ID: P0-PAPER-CLOSE-EXIT-PRICE-HONESTY
Current Branch: fix/paper_close_exit_price_honesty
Current Commit: pending-handoff-commit
Working Tree Status: DIRTY-UNTIL-COMMIT
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: 191e26086fb5f97b586fd6a2bd16f28185ae44a9509c78924f71d272f526c921
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — P0 paper-close exit-price honesty defect is fixed on branch
   `fix/paper_close_exit_price_honesty` (base `main@aaf38f566d9a61dd8b2a85686461ec0a58f165de`,
   verified before implementation). Staging evidence (close audit event
   `4442c6ac-9fac-4e5f-ae0a-8f66697bbc31`): an explicit paper close submitted `91234.56` but the
   audit event recorded `exit_price=64524.01000000` and `realized_pnl=0.07955` — the market ticker
   silently replaced the explicit submitted price. The defect was reproduced locally with a
   non-mock provider configuration, reproducing the exact staging audit values, before any code
   change. Explicit submitted paper close prices are now authoritative end to end. No live
   trading enabled, no deploy, PR #41 untouched, kill switch unchanged.

2. **Goal** — Make an explicit user-submitted paper close price authoritative for persistence,
   PnL, and audit; keep market-data-bound fail-closed behaviour for closes without an explicit
   price; add regression coverage; investigate journal linkage and the portfolio staging e2e
   failure.

3. **Current status** — `REVIEW_REQUIRED`. Implementation and validation complete; draft PR
   opened for human review. Do not merge or deploy without human approval.

4. **Current branch, commit, and phase** — `fix/paper_close_exit_price_honesty`; fix commit
   `729e728`, e2e-test commit `2089519`, CI-flake test fix `b6a6da4`, plus handoff commits.
   Phase: implementation complete, PR review pending (draft PR #60).

5. **Last completed task** — Full validation matrix (backend, frontend, deployment safety,
   evaluation, browser smoke) on the fixed branch.

6. **Architecture summary** — Root cause: `PositionService._resolve_close_exit_price`
   (`backend/src/app/services/position_service.py`), introduced in commit `7ebe3b0` (AT-012),
   returned `ticker.last_price` for every close whenever `provider_mode`/`market_data_provider`
   were non-mock, discarding the explicit `requested` price. Fix: an explicit submitted price
   (always present on the `/positions/{id}/close-paper` request per `ClosePaperPositionRequest`)
   is returned directly after fail-closed validation (positive, finite Decimal); market data is
   not consulted for explicit closes, so stale/unavailable quotes can neither replace nor refuse
   them. Closes with `requested=None` (future system/automated paths) still bind to a fresh
   server ticker and fail closed (`market_data_unavailable` / `market_data_degraded`); no price
   is ever fabricated. Audit event now also records `exit_price_source=user_submitted`.
   Secondary fix: `app/core/errors.py` request-validation handler now encodes pydantic error
   payloads with `jsonable_encoder`, so malformed Decimal input returns a clean 422 instead of a
   500 (surfaced by the new endpoint regression test).

7. **Files changed** —
   - `backend/src/app/services/position_service.py` (fix)
   - `backend/src/app/core/errors.py` (422 encoding fix)
   - `backend/tests/test_paper_close_exit_price_honesty.py` (new, 21 tests)
   - `backend/tests/test_at012_paper_risk_at_execution.py` (close-binding test updated to the
     new contract)
   - `frontend/e2e/portfolio-staging.spec.ts` (stale banner expectation aligned with FP2-123)
   - `frontend/src/app/(app)/strategy-lab/[id]/page.test.tsx` (pre-existing timing flake in the
     retry test surfaced on CI run 31105503067; failed status now awaited)
   - `HANDOFF.md`, `CHANGELOG_SESSION.md` (committed on explicit task instruction)

8. **Important code references** —
   - `PositionService.close_paper` / `_resolve_close_exit_price` / `_market_exit_price` /
     `_validated_explicit_exit_price` in `backend/src/app/services/position_service.py`
   - `shouldSuppressPaperPortfolioSafetyBanner` (FP2-123) in
     `frontend/src/components/portfolio/PaperPortfolioSafetyBanner.tsx`
   - AT-033 journal flag `journal_auto_from_position_close` in `backend/src/app/core/config.py`

9. **Tests and validation** —
   - Targeted: `uv run pytest tests/test_paper_close_exit_price_honesty.py
     tests/test_at012_paper_risk_at_execution.py` → 38 passed, exit 0.
   - Full backend: `uv run pytest -q` → 1394 passed, 11 skipped, exit 0.
   - Lint/format: `uv run ruff check src tests` and `uv run ruff format --check src tests` →
     pass. `uv run mypy src` → 375 pre-existing errors repo-wide, none in the changed files
     (only pre-existing `dict` type-arg note on untouched `_error_body`).
   - Deployment safety: `uv run pytest tests/test_deployment_safety.py
     tests/test_deployment_scripts.py tests/test_config.py` → 48 passed;
     `scripts/post-deploy-smoke-gate.sh --self-check` → pass.
   - Evaluation: agent 16/16, RAG 5/5, guardrails 7/7 → pass.
   - Frontend: `npm run lint` pass, `npm run typecheck` pass, `npm run test` 1137 passed (184
     files), `npm run build` pass.
   - Browser: `npm run test:e2e` (chromium) → 20 passed, 13 skipped (staging-gated specs,
     including the updated portfolio-staging spec, require staging env flags).
   - Docker build job: not runnable locally (no docker in the VM); covered by GitHub CI.
   - GitHub CI (all six jobs) runs on the exact pushed head; results recorded in the PR.

10. **Deployment and provider status** — No deploy performed. No production or staging
    configuration changed. `render.yaml` untouched.

11. **Security and trading safety status** — `EXECUTION_MODE=paper`,
    `ENABLE_REAL_TRADING=false`, `EXCHANGE_MODE=paper_internal` defaults unchanged and asserted
    in tests. No live order or exchange execution capability added; explicit closes now touch
    no market/exchange path at all (asserted in tests). Kill-switch authority unchanged
    (AT-012 kill-switch tests still pass). No fallback price literal introduced; missing/
    degraded data fails closed for non-explicit closes. No secrets logged or committed.

12. **Decisions confirmed** —
    - Explicit paper close prices are authoritative (P0 task mandate); the AT-012 anti-
      manipulation ticker binding now applies only to closes without an explicit price.
    - Journal linkage verdict: NOT a defect in the close path. AT-033 auto-journaling on paper
      close is opt-in (`journal_auto_from_position_close` defaults to `false`;
      `test_flag_off_by_default_no_auto_journal` codifies this) and staging (`render.yaml`)
      does not enable it, so the staging close correctly produced no journal linkage. If
      staging should link journals automatically, enable the flag there — separate follow-up
      product/config decision, no code change made.
    - Portfolio staging e2e verdict: application is correct; the test expectation was stale.
      FP2-123 intentionally suppresses the redundant `paper-portfolio-safety-banner` when the
      payload confirms the standard verified paper posture (StatusStrip + PaperModeIndicator
      remain). Unit tests already codified this; only the e2e expectation was updated, now
      branching on the actual safety payload.

13. **Known issues and blockers** — None blocking. Follow-ups: (a) decide whether staging
    should enable `JOURNAL_AUTO_FROM_POSITION_CLOSE`; (b) pre-existing repo-wide mypy debt
    (375 errors) untouched; (c) staging re-validation of the exit-price drill requires merge +
    redeploy (human action).

14. **Remaining prioritized tasks** — Human PR review/merge; staging redeploy; re-run staging
    exit-price, journal, and portfolio browser gates; physical iPhone validation (from prior
    handoff) still outstanding.

15. **Recommended model** — Any current Cursor default model; no special capability required.

16. **Exact next instruction for ChatGPT** — Review the draft PR for branch
    `fix/paper_close_exit_price_honesty` (paper-close exit-price honesty), confirm the safety
    analysis in the PR description, and approve or request changes. Do not merge without
    reviewing the audit-event contract change (`exit_price_source` added).

17. **Exact next Cursor prompt** —

```
TARGET: STAGING RE-VALIDATION — paper close exit price honesty
After the fix/paper_close_exit_price_honesty PR is approved, merged to main, and staging is redeployed:
1. Re-run the staging exit-price drill: open a paper position, close it with a distinctive explicit exit price, and verify the close audit event has exit_price == requested_exit_price, exit_price_source=user_submitted, and realized PnL derived from the submitted price.
2. Re-run the kill-switch drill and confirm authority is unchanged.
3. Re-run the portfolio staging browser gate (PLAYWRIGHT_STAGING_PORTFOLIO_SMOKE=1) against the updated spec.
4. Decide and record whether staging should set JOURNAL_AUTO_FROM_POSITION_CLOSE=true (AT-033 opt-in) for journal linkage after paper closes.
Do not enable real trading. Do not change production configuration.
```
