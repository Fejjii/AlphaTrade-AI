Status: READY
Last Updated: 2026-09-21T08:55:01+00:00
Task: AT-063 — Second independent adversarial review of PR113
Current Phase: REVIEW COMPLETE — no product code changed
Progress: Independent re-verification of 19 previous P1s against PR113 HEAD dfd0a8a; verdict REMEDIATION REQUIRED
Blocker: None
Human Action Needed: Do not merge PR113. Do not deploy. Do not activate Watcher, Telegram, or live trading. Close remaining OPEN P1s before any Watcher implementation phase.
Next Step: Remediate OPEN P1s (product-path expiry, chat confirm identity, invented StructuredRules/paper-bot authority, in-memory Candidate mint) then re-review.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-21T08:55:01+00:00
Generated At UTC: 2026-09-21T08:55:01Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260921-085501
Task ID: AT-063
Current Branch: cursor/intelligence-remediation-final-review-051b
Current Commit: dfd0a8a
Working Tree Status: DIRTY
Generated By: Cursor
Handoff Status: READY
Source File SHA256: ca8c19d5dd0fee37163b413707674e34c8814e02e217ab553c7e0a43e8abcfa9
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Second independent adversarial review of PR113
   (`cursor/intelligence-integration-remediation-6a59` @ `dfd0a8a`, stacked on
   PR112 @ `1049225`). Review only. No product-code changes, merge, deploy,
   Watcher, Telegram, or live trading. Original PR112 review was REMEDIATION
   REQUIRED (0 P0, 9 P1). PR113 closed the HTTP compile/approve ladder, CVD
   observation binding, `observed_at` hash exclusion, Watcher DB resolve, and
   placeholder Candidate blocks. It did **not** close product-path setup expiry,
   chat confirmation identity, invented StructuredRules feeding paper-bot, or
   in-memory Candidate minting. The claimed product loop is real through
   persisted executable policy; evidence → SetupAssessment is still a service
   stitch, and GET `/canonical/evidence` remains a placeholder read projection.

   **FINAL VERDICT: REMEDIATION REQUIRED**

2. **Goal** — Independently reverify every previous P1 against repository
   evidence; falsify discussion → preview → confirm → draft → compile/review →
   approval → executable policy → canonical evidence → SetupAssessment on real
   HTTP/service boundaries; assess Watcher-readiness without activating it.

3. **Current status** — `READY` (review task complete). Product repository
   unchanged. PR113 remains draft. Do not merge.

4. **Current branch, commit, and phase** — Review working tree at PR113 HEAD
   `dfd0a8a`. Local review branch
   `cursor/intelligence-remediation-final-review-051b` tracks PR113 and was
   **not** pushed (would pollute PR113). Handoff published on a dedicated
   `cursor/handoff-*` branch only.

5. **Last completed task** — Independent PR113 adversarial review.

6. **Architecture summary** — Real HTTP now exists for confirm, compile
   (`REVIEW_REQUIRED`), and approve (`APPROVED`). `resolve_executable_strategy_policy`
   still fail-closes drafts and hash mismatches. `evaluate_setup` now binds
   observation type + `payload_content_hash` to consumed CVD/flow/coverage.
   Watcher scan evidence prefers DB resolve when session+store are present.
   Remaining split authorities: paper-bot `scan`/`tick`, injected Watcher
   resolver, in-memory `executable_policy_from_fusion_policy`, GET evidence
   placeholders, module `POST /strategies/evaluate`.

7. **Files changed** — None (review-only). Gitignored handoff artifacts only.

8. **Important code references** —
   - Compile/approve HTTP: `backend/src/app/api/routes/strategy_library.py:369-414`
   - Compile/approve service: `backend/src/app/services/compiled_setup_service.py`
   - Confirm identity: `strategy_proposal_service.py:_assert_confirmation_identity`
   - Chat confirm tautology: `tools/registry.py:1557-1572`, `agents/nodes.py:1181-1183`
   - Observation binding: `signal_fusion/evaluator.py:_bind_consumed_observations`
   - Transport exclusions: `market_contracts/hashing.py:RECEIVE_AND_TRANSPORT_FIELDS`
   - Watcher resolve: `evidence_pipeline/watcher_port.py:resolve_watcher_scan_policy`
   - Expiry pin missing: `evidence_pipeline/service.py:_setup`, `watcher_port.py:load`
   - Paper-bot: `paper_validation_runtime_service.py:scan` + `POST /paper-validation/{run_id}/scan`
   - PR113: https://github.com/Fejjii/AlphaTrade-AI/pull/113
   - Prior review agent: https://cursor.com/agents/bc-01a0c090-c4f1-7ef7-b4d4-608421b7868e

9. **Tests and validation** —
   - This run did not execute local pytest (no project venv in the cloud
     snapshot). CI was independently checked, not taken from the PR body:
     GitHub Actions run 35575538916 **success** at exact HEAD `dfd0a8a`.
   - Passing CI is not proof of the claimed product loop. The fixture drives
     real HTTP through approve, then in-process assembler +
     `evaluate_canonical_strategy` twice on the **same objects**.
   - `test_incomplete_and_unsupported_cannot_become_executable_policy` can
     return early without asserting fail-closed.

10. **Deployment and provider status** — No deploy. Watcher worker has no
    import of `AssemblingWatcherScanEvidence`. GET `/canonical/evidence`
    hardcodes `live_executable: false` and `watcher_activated: false`.

11. **Security and trading safety status** — Paper-only pins unchanged
    (`EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`). Staging/production
    still refuse Watcher/Telegram/real trading. This review did not activate
    any of those paths.

12. **Decisions confirmed** — Review only. Do not merge PR113. Do not start
    Watcher activation. Remaining OPEN P1s are merge blockers for the claimed
    intelligence loop, not a redesign of confirm→compile→approve.

13. **Known issues and blockers** — See OPEN P1s below. No environmental
    blocker.

14. **Remaining prioritized tasks** — Close OPEN P1s on a new remediation
    branch; do not activate Watcher; keep paper-bot from executing
    conversation-confirmed invented rules; pin setup_trigger_end on product
    assembler/Watcher paths; bind chat confirm to client-presented identity.

15. **Recommended model** — Cursor Grok 4.6 Extra High

16. **Exact next instruction for ChatGPT** — Treat PR113 as **not mergeable**.
    Schedule a remediation that closes the four OPEN P1s. Do not plan Watcher
    activation. Do not treat CI 35575538916 as loop proof.

17. **Exact next Cursor prompt** —
    Review-only is complete. Next authorized implementation task (separate):
    remediate PR113 OPEN P1s only — (1) product assembler/Watcher must pin
    `setup_trigger_end` so setup expiry can fire without a test-only argument;
    (2) chat confirm must re-assert client-presented proposal identity, not
    fill `expected_*` from the stored row / `pending_proposal_id`; (3) invented
    StructuredRules must not become `current_version` for paper-bot `scan`/`tick`
    unless they also satisfy executable pattern_spec, or paper-bot must refuse
    non-approved compiled lineage; (4) in-memory/injected Watcher policies must
    not mint Candidates. Do not merge. Do not deploy. Do not activate Watcher.

---

## Product loop (falsified against HTTP/service boundaries)

| Step | Supported on product boundary? | Evidence |
| --- | --- | --- |
| Discussion | Yes, HTTP | `POST /chat/message` |
| Preview | Yes, HTTP | `POST /conversations/{id}/proposals` |
| Explicit confirmation | HTTP yes; chat weaker | HTTP sends expected hash/parent/target. Chat `I confirm` binds `pending_proposal_id` and fills `expected_*` from the DB row (`registry.py:1557-1572`). |
| Immutable draft | Yes | Confirm forks a version; does not compile or approve. |
| Explicit compile/review | Yes, HTTP | `POST /strategies/{id}/versions/{vid}/compile` → `REVIEW_REQUIRED` |
| Explicit approval | Yes, HTTP | `POST .../approve` requires executable compile + confirm phrase. UI Approve disabled until `compileStatus === "executable"` (in-memory; lost on reload). |
| Persisted executable policy | Yes, service | `resolve_executable_strategy_policy` requires APPROVED/ACTIVE + hash match. |
| Canonical evidence | Read HTTP is **not** that policy | `GET /canonical/evidence` uses `first_slice_read_policy` placeholders (`service.py:_setup`). |
| Deterministic SetupAssessment | Service only | No product HTTP evaluate. Fixture evaluates in-process twice on the same assembled objects. |

The authority ladder through **persisted executable policy is genuine HTTP**.
The advertised close **canonical evidence → deterministic SetupAssessment**
is **not** a user-reachable HTTP loop. Watcher would use the assembling
service port, which is unwired and still omits expiry pinning.

## Previous P1s — CLOSED or OPEN

1. Canonical semantic observation binding — **CLOSED**.
   `evaluator.py:_payload_matches` / `_bind_consumed_observations` require
   type + `payload_content_hash` vs role. Dishonest OHLCV in `CVD_WINDOW`
   cannot reach `CONFIRMED_SETUP` (`test_canonical_observation_binding.py:55-78`).

2. CVD, flow and coverage identity — **CLOSED**.
   Evaluator recomputes CVD/flow/coverage from snapshot then binds those hashes
   to command observations. Product `MANDATORY_ROLES` include SIGNED_FLOW and
   TRADE_EVENT (`canonical.py:51-57`). Semantic CVD fail does not confirm
   (`test_canonical_observation_binding.py:32-52`).

3. Semantic hash stability across retry/restart/transport — **CLOSED**.
   `observed_at`, connection IDs, receive times, lineage/scan/action IDs are in
   `RECEIVE_AND_TRANSPORT_FIELDS` (`hashing.py:18-46`). Tests:
   `test_live_hash_stability_and_expiry.py` connection/restart/later clock.

4. Relevant market corrections changing identity — **CLOSED**.
   Trade/CVD correction with unchanged candles forks window hash
   (`test_live_evidence_pipeline.py`); candle revision forks
   (`test_market_correction_changes_identity`).

5. Persisted approved strategy resolution for Watcher — **CLOSED**.
   `resolve_watcher_scan_policy` calls `resolve_executable_strategy_policy`,
   refuses disabled versions, prefers session+store over injected resolver
   (`watcher_port.py:28-52, 121-124`). Fixture injects
   `executable_resolver=lambda _: None` and still loads via DB.

6. Placeholder/in-memory policies unable to mint Candidate authority — **OPEN**.
   Placeholders are blocked at assembling port, fusion persist, and
   `CandidateLifecycleService._validate_creation_authority` — that half is
   closed. `executable_policy_from_fusion_policy` still defaults
   `lifecycle_state=APPROVED`. Injected resolver still works when session/store
   are absent. Fusion evaluates `snapshot.executable_policy` and does not
   re-resolve (`fusion_evaluation.py:341-349`). Test composition can still
   mint Candidates from in-memory APPROVED helpers.

7. Conversation confirmation identity — **OPEN**.
   HTTP confirm binds conversation, proposal, content hash, parent, target,
   user, org (`conversations.py:182-199`,
   `_assert_confirmation_identity`). Chat confirm still selects
   `pending_proposal_id` for bare `I confirm` (`nodes.py:1181-1183`) and the
   tool fills expected hashes from the stored row (`registry.py:1557-1572`),
   making identity a self-check. A newer auto-draft can be the confirmed
   object.

8. Concurrent confirmation convergence — **CLOSED** (P2 residual).
   Unique `proposal_id` link + `IntegrityError` recovery
   (`strategy_proposal_service.py:291-311`; Alembic
   `uq_strategy_proposal_version_link`). Test is still SQLite threads
   (`test_concurrent_confirmation_converges_to_one_version`). No Postgres
   `FOR UPDATE` proof.

9. Explicit compile/review step — **CLOSED**.
   HTTP compile persists `CompiledSetupDefinition` and appends
   `REVIEW_REQUIRED` from DRAFT (`compiled_setup_service.py:106-120`).
   No compile chat tool.

10. Explicit approval step — **CLOSED**.
    Separate HTTP approve; conversation confirm is not approval
    (`compiled_setup_service.py:141-145`). Requires executable compile first.

11. Approved compiled hash binding — **CLOSED**.
    Approve recompiles and compares hash (`compiled_setup_service.py:173-183`).
    Resolve recompiles (`canonical_strategy_evaluation.py:105-126`).
    `persist_compiled` refuses a different hash for the same version.

12. Unsupported/incomplete strategies fail closed — **OPEN**.
    Canonical compile/resolve fail without `pattern_spec`
    (`setup_ast_compiler.py:734-746`; `canonical_strategy_evaluation.py:177-189`).
    Confirm still requires only `proposed_structured_rules`
    (`strategy_proposal_service.py:283-284`). `draft_preview` still invents
    EMA/LONG/2%/1R StructuredRules (`structure_from_text_service.py:111-171`).
    Those versions become `UserStrategy.current_version`. Paper-bot
    `POST /paper-validation/{run_id}/scan` still uses `resolve_backtest_rules`
    / setup defaults and can open AUTO_PAPER trades. The fixture's
    unsupported HTTP test returns early if structured rules are missing or
    confirm fails (`test_intelligence_integration_fixture.py:306-313`).

13. Quote freshness vs trade freshness vs candle finality — **CLOSED**.
    Separate clocks in `EvidenceClockReport`; `FIRST_SLICE_TRADE_MAX_AGE_SECONDS`
    remains 10; live window vs historical closed-bar split is tested.
    Residual P2: assembler stamps observations `FRESH`, so evaluator
    `stale_obs` is dead on that path; live age is still enforced in CVD/flow
    compute.

14. Historical evidence validity and setup expiry — **OPEN**.
    Expiry needs `subsequent_final_15m`. Assembler only fills that when
    `setup_trigger_end` is set (`assembler.py:300-312`). Product callers
    omit it: `CanonicalEvidenceService._setup` (`service.py:183-188`) and
    `AssemblingWatcherScanEvidence.load` (`watcher_port.py:96-101`).
    `test_quote_trade_and_setup_clocks_stay_separated` asserts
    `subsequent_final_15m_count == 0` even later in time. Expiry is only
    demonstrated with a manual evidence patch or explicit pin.

15. Tenant isolation — **CLOSED**.
    Confirm is org+user+conversation scoped. Compile/approve require org +
    `strategy.user_id` + path `strategy_id`. Watcher resolve raises
    `WatcherTenantMismatchError` on org mismatch. Missing tests: HTTP
    cross-tenant compile/approve (code-level checks exist).

16. Restart determinism — **CLOSED**.
    Replay restart/retry converges (`test_restart_retry_same_semantic_batch_converges`).
    Live `observed_at` no longer forks semantic hashes.

17. Prompt/RAG/assistant text cannot authorize mutations — **CLOSED**.
    `confirmation_authorizes_mutation` rejects questions, quotes, retrieved
    text. Fixture `"> I confirm"` → 422. Compile/approve are not chat tools.
    Residual P2: quote-style gaps from the first review.

18. Alembic integrity — **CLOSED**.
    PR113 adds no migration. Head remains `b7c8d9e0f1a2` revises
    `e8f1c4a9b702`. Unique version/proposal links exist. Residual P2: no
    unique on `confirmation_request_id`; enum names vs lowercase values.

19. Paper-only safety — **CLOSED**.
    Defaults and `deployment_safety` / `paper_safety` / `exchange_safety` pins
    unchanged vs the intelligence claim. Watcher worker unwired. Review did
    not enable Watcher, Telegram, or live trading.

## P0 / P1 / P2 (this review)

### P0
None on currently wired paths. Unsupported/incomplete/stale inputs still
cannot reach canonical `CONFIRMED_SETUP` → Candidate → Telegram → live
order. That is not the same as “the claimed loop is proven” or “Watcher is
safe to implement from this composition.”

### P1 (open now)
1. Product assembler / Watcher / GET-evidence setup path never pins
   `setup_trigger_end`, so setup expiry cannot fire (previous P1 #14).
2. Chat confirm identity is still tautological (`pending_proposal_id` +
   server-filled expected hashes) (previous P1 #7).
3. Conversation confirm still persists invented StructuredRules as
   `current_version`; paper-bot `scan`/`tick` remains an armed second
   evaluation authority (previous P1 #12 / original P1-7 and P1-9).
4. In-memory / injected Watcher policies can still mint Candidates when
   session+store are absent; fusion does not re-resolve DB policy
   (previous P1 #6 residual).

### P2
- GET `/canonical/evidence` is a placeholder read projection, not approved
  compiled evaluation.
- Fixture SetupAssessment proof is same-object in-process purity.
- UI `compileStatus` is React state; reload hides Approve until compile is
  clicked again.
- E2E `strategy-conversation.spec.ts` still proves discussion persistence,
  not confirm/compile/approve/tenant.
- Incomplete/unsupported HTTP test can pass vacuously via early return.
- `append_lifecycle` has no transition table; `ACTIVE` has no prior-APPROVED
  check.
- Concurrent confirm untested on Postgres.
- No unique `confirmation_request_id`.
- `compile_status` ORM default remains `EXECUTABLE`.
- `phase6_fusion.MANDATORY_ROLES` still omits SIGNED_FLOW / TRADE_EVENT
  versus product `canonical.MANDATORY_ROLES`.
- `make_command` dishonest fallback still puts trigger OHLCV in CVD_WINDOW
  when snapshot is omitted (evaluator now fail-closes that case).
- Trader (not Owner-only) can compile and approve.
- Full-repo mypy remains dirty (not re-measured).

## Missing tests
- Product-path expiry without passing `setup_trigger_end` in the test.
- Chat confirm vs a newer auto-draft than the one the user previewed.
- Postgres concurrent confirm.
- HTTP cross-tenant compile/approve.
- Incomplete/unsupported HTTP without early return; assert version is not
  paper-bot-executable.
- SIGNED_FLOW / TRADE_EVENT wrong-type binding cases.
- Browser e2e for confirm → compile → approve.
- Independent re-assembly (second assembler instance) in the intelligence
  fixture.
- Fusion refuse injected-resolver Candidate mint in a production-shaped
  composition.

## Remaining duplicate authorities
| Path | Bound to approved compiled version? | Armed now? |
| --- | --- | --- |
| `resolve` → `evaluate_canonical_strategy` | Yes | Service; no HTTP evaluate |
| GET `/canonical/evidence` placeholders | No (by design) | Yes, read-only |
| Watcher + session/store | Yes | Unwired |
| Watcher + injected resolver / in-memory APPROVED helper | No | Tests only; copy-paste hazard |
| Paper-bot `scan`/`tick` | No | Yes, HTTP |
| `POST /strategies/evaluate` modules | No | Yes, HTTP |
| Direct `evaluate_setup` | No | Public export / tests |
| Worker `analyze()` | No | Loop off |

## Deployment implications
Merging PR113 while Watcher/Telegram/live trading stay disabled would not
enable real trading. It would still publish an overstated product loop
(evidence→assessment, chat identity, paper-bot split, expiry). Do not merge.
Do not deploy. Accidental Watcher flag on staging/production still refuses
process start.

## Watcher-readiness implications
**Not safe to activate. Not safe to treat current wiring as the Watcher
implementation.** A later **separate** paper-monitoring Watcher phase may be
planned only if it is required to close OPEN P1s 1 and 4 (and must not copy
injected-resolver test composition). Do **not** enable
`WATCHER_ORCHESTRATION_ENABLED`. Do not wire the worker in this PR.

## FINAL VERDICT
**REMEDIATION REQUIRED**
