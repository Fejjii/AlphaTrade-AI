Status: IN_PROGRESS
Last Updated: 2026-09-24T13:54:03+00:00
Task: AT-085 — Binance USD-M HTTP 418 recovery
Current Phase: Exact-head CI rerun
Progress: The three provider_outage tests pass on 75a5273, including the Postgres loop. Exact-head CI run 36007823166 is in progress. Backend is still running. Frontend, deployment-safety, and docker-build succeeded.
Blocker: None
Human Action Needed: None
Next Step: Wait for backend CI on 75a5273. Do not deploy or activate.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-24T13:54:03+00:00
Generated At UTC: 2026-09-24T13:54:03Z
Timezone: UTC
Session ID: AT-SESSION-20260924-123856
Task ID: AT-085
Current Branch: cursor/binance-418-recovery-11ce
Current Commit: 75a5273
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: IN_PROGRESS
Source File SHA256: 4a4389c1214bed511ef52ed2ce1242c1408c56844f1bec7193d933ef797ad8c9
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — The `provider_outage` regression from CI run 36000366161 is fixed on `75a5273`. A regional failure stays classified as `PROVIDER_UNAVAILABLE` through later incomplete recovery, so Watcher still returns `provider_outage`. An in-backoff wait stays degraded. HTTP 418 recovery behavior is unchanged. Exact-head CI for this commit is not finished, so the staging verdict is not set.
2. **Goal** — Keep provider outages classified as `provider_outage` without weakening the tests or the HTTP 418 recovery behavior.
3. **Current status** — IN_PROGRESS. No new commit. Working tree clean. CI run 36007823166 on `75a527349b27801e33445642bcb3212d4947f0a7` is in progress.
4. **Current branch, commit, and phase** — `cursor/binance-418-recovery-11ce` at `75a5273`. Draft PR https://github.com/Fejjii/AlphaTrade-AI/pull/138.
5. **Last completed task** — Re-ran the three previously failing tests with Postgres available. All three passed. Targeted Binance recovery tests, ruff, and strict mypy on the six edited modules passed.
6. **Architecture summary** — HTTP 418 is `UpstreamBanError` with bounded retry. 401, 403, and 451 stay regional. 429 stays rate-limited. Incomplete backfill stays on the same cursor and connection and does not publish a usable price. While a reconnect was opened by `RegionalProviderFailureError`, the projected reason stays `PROVIDER_UNAVAILABLE`. The backoff-only tick is degraded. A failed recovery attempt is unavailable.
7. **Files changed** — No files changed in this confirmation pass. The classification fix is `f883f3b` and `75a5273` in `backend/src/app/market_monitor/runtime.py`.
8. **Important code references** — `SymbolMonitorRuntime._provider_outage_open` and `_availability` in `runtime.py`. `watcher_evidence_error_for_monitor` in `watcher_gate.py`.
9. **Tests and validation** — `uv run pytest` on `test_postgres_fail_closed_paths_do_not_mint_or_override_authority`, `test_provider_outage_monitor_blocks_assembler`, and `test_provider_outage_cannot_mint_candidate` exit 0 (3 passed) after PostgreSQL 16 was started locally. Targeted recovery pytest exit 0 (17 passed). `uv run ruff check` and `uv run ruff format --check` on the edited modules exit 0. `uv run mypy --strict` on the six edited modules exit 0. CI run 36007823166: frontend success, deployment-safety success, docker-build success, backend in progress. e2e-smoke and evaluation had not started.
10. **Deployment and provider status** — Not deployed. Watcher and Telegram were not activated. Live trading was not enabled.
11. **Security and trading safety status** — Paper only. No secrets. No spot fallback. No fabricated fallback. No stale usable price while coverage is incomplete.
12. **Decisions confirmed** — AT-ADR-066. Provider outage stays `provider_outage`. Incomplete evidence that is not a provider outage can stay `canonical_evidence_unavailable`.
13. **Known issues and blockers** — Exact-head backend CI for `75a5273` is still running.
14. **Remaining prioritized tasks** — Wait for CI run 36007823166. Do not merge, deploy, or activate.
15. **Recommended model** — Grok 4.7
16. **Exact next instruction for ChatGPT** — Do not deploy or activate. Read CI run 36007823166 before a staging-revalidation verdict.
17. **Exact next Cursor prompt** — Read https://github.com/Fejjii/AlphaTrade-AI/actions/runs/36007823166 for commit `75a5273`. If every required check is green, set this handoff to READY and state READY FOR STAGING REVALIDATION. If a check failed, fix only that failure. Do not deploy. Do not activate Watcher or Telegram. Do not enable live trading.

