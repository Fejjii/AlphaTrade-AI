Status: REVIEW_REQUIRED
Last Updated: 2026-09-25T13:13:41+00:00
Task: AT-070 — Failover provider kind staging boot
Current Phase: Review
Progress: Fix committed on 0cdc410; focused checks passed; draft PR 143 open
Blocker: None
Human Action Needed: Review and merge PR 143. Do not deploy, arm Watcher, or arm Telegram.
Next Step: Merge PR 143 so Render API startup can register the Binance/Bybit failover.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-25T13:13:41+00:00
Generated At UTC: 2026-09-25T13:13:41Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260925-131341
Task ID: AT-070
Current Branch: cursor/staging-boot-failover-kind-98bd
Current Commit: 0cdc410
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: d35705b4571ea80e0be4dc535657630b41acb71af4eec0c1a4b0463ab03faaa6
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Staging boot on main `ecd6c374` failed because `FailoverPerpetualSource` had no `kind` and `_PerpetualEvidenceAdapter` copies `inner.kind`. The wrapper now reports the active source's provider kind. Both Binance USD-M and Bybit USDT perpetual must report `ProviderKind.MARKET_DATA`. Verdict: STAGING BOOT FIX READY.
2. **Goal** — Restore Render API startup for `PERPETUAL_EVIDENCE_SOURCE=binance_usdm` and `PERPETUAL_EVIDENCE_SECONDARY_SOURCE=bybit_usdt_perpetual` without weakening paper-only safety.
3. **Current status** — REVIEW_REQUIRED. Code and regression are on draft PR 143. A human merge is required before staging picks up the fix. This run did not deploy.
4. **Current branch, commit, and phase** — `cursor/staging-boot-failover-kind-98bd` at `0cdc410788a0f85f8e494f77b7f49b5f10a8cd4d`, based on `ecd6c374972636011dc0cf603c56fbbec9280c66`. Phase: review.
5. **Last completed task** — Provider-kind contract for the perpetual failover, plus a startup regression that builds the staging provider registry and `create_app`.
6. **Architecture summary** — `kind` is the provider capability (`market_data`), not the venue. Construction reads both sources and refuses any other kind. Switch and primary recovery copy `kind` from the source that becomes active. Venue provenance stays on the instrument and source identity. No spot fallback and no fabricated evidence.
7. **Files changed** — `backend/src/app/market_contracts/adapters/failover.py`, `backend/src/app/market_contracts/adapters/protocol.py`, `backend/tests/test_bybit_usdt_perpetual_evidence.py`, `docs/market_source_contracts.md`, `.ai/DECISIONS.md` (AT-ADR-070).
8. **Important code references** — `FailoverPerpetualSource.__init__` calls `_require_market_data_kind`. `build_default_registry` still registers `_PerpetualEvidenceAdapter` unchanged. `PerpetualMarketSource` now requires `kind`.
9. **Tests and validation** — From `backend/`: `uv run pytest tests/test_bybit_usdt_perpetual_evidence.py tests/test_phase5_perpetual_adapter.py tests/test_provider_integration.py tests/test_providers_status.py tests/test_live_market_activation.py tests/test_deployment_safety.py tests/test_deployment_scripts.py tests/test_config.py tests/test_watcher_paper_activation.py` exited 0 (166 passed, 18.26s). `uv run ruff check .` and `uv run ruff format --check .` exited 0. `uv run mypy --strict src/app/market_contracts` exited 0 (30 files). Affected mypy on registry still reports four pre-existing email/billing adapter errors that also exist on main; the main-only error that the failover was not a `_StatusReporter` is gone. Full backend suite was not run.
10. **Deployment and provider status** — Not deployed. Watcher and Telegram were not armed. No live Bybit or Binance probe was required; the regression uses mock provider mode and does not call provider `status()` (that probe would hit the network).
11. **Security and trading safety status** — `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`, `EXCHANGE_MODE=paper_internal`. No live orders, withdrawals, leverage changes, or exchange mutations.
12. **Decisions confirmed** — AT-ADR-070. Kind is the shared market-data capability of the active source.
13. **Known issues and blockers** — None for this fix. Staging remains on main until PR 143 is merged. This VM cannot treat a live Bybit call as evidence (CloudFront block on earlier runs).
14. **Remaining prioritized tasks** — Human review and merge of PR 143. Confirm the next Render API boot no longer raises the `kind` AttributeError.
15. **Recommended model** — A coding model that can read the provider registry and perpetual adapters.
16. **Exact next instruction for ChatGPT** — PR 143 fixes staging startup by giving `FailoverPerpetualSource` the active source's `market_data` kind. Do not deploy, arm Watcher, or arm Telegram. Merge is a human action.
17. **Exact next Cursor prompt** — After PR 143 is merged, confirm the Render API boot log no longer contains `FailoverPerpetualSource` / `has no attribute 'kind'`. Do not deploy, arm Watcher, arm Telegram, or enable real trading.

- exact blocker: None. Human merge is required to change staging.
- exact command or action that failed: Render API startup on main `ecd6c374` in `build_default_registry` → `_PerpetualEvidenceAdapter` → `self.kind = inner.kind`.
- concise relevant error output: `AttributeError: 'FailoverPerpetualSource' object has no attribute 'kind'`
- what was already completed: Contract fix, regression, ruff, affected mypy, focused tests, draft PR 143.
- what remains unchanged: Watcher authority, Telegram, real trading, spot rejection, fabricated-evidence rejection.
- risk of continuing: Merging is the remaining step. Further code changes are not required for this boot error.
- whether application code was modified: Yes.
- whether uncommitted work exists: No on `cursor/staging-boot-failover-kind-98bd`.
- exact human action required: Review and merge https://github.com/Fejjii/AlphaTrade-AI/pull/143. Do not deploy or arm Watcher or Telegram as part of that merge.
- exact Cursor instruction after resolution: Confirm the next Render API startup log no longer shows the missing `kind` AttributeError.
