Status: BLOCKED
Last Updated: 2026-09-25T09:55:49+00:00
Task: Post-merge paper MVP activation runbook (read-only)
Current Phase: Reconcile main + PR 140; no deploy
Progress: Checklist written; full trade loop has no operator command
Blocker: No HTTP route creates a canonical TradePlan or records the paper fill required before close
Human Action Needed: Do not start the 16-step activation. Decide whether to authorize an operator-safe plan and fill path before any staging arm.
Next Step: Wait for that decision. Do not merge, deploy, or arm from this run.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-25T09:55:49+00:00
Generated At UTC: 2026-09-25T09:55:49Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260925-095549
Task ID: NONE
Current Branch: cursor/paper-mvp-activation-handoff-a9ec
Current Commit: 8591b8d
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: BLOCKED
Source File SHA256: 50beaf0628ddbef41ee58bde0bff9a316bf91ee40a80fcc75e786017b345489c
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Read-only reconciliation of `main` (`8591b8d`, Telegram MVP PR 139 and Stats MVP PR 141) plus open PR 140 (`1fd28ee`, Binance USD-M primary, Bybit linear USDT perpetual secondary). The activation checklist below is exact through disarmed deploy and staged Watcher/Telegram arming. It is not an executable full paper-trade loop. No product code was modified. Nothing was deployed or armed.

2. **Goal** — One sequential post-merge checklist for merge, Render env, API deploy, combined paper worker deploy, Binance/Bybit BTCUSDT evidence, Watcher arm, continuous scans, Telegram enrollment, Telegram arm, first canonical paper trade, close, Journal, PnL/win-loss/statistics, learning/evaluation, Telegram discussion, and rollback.

3. **Current status** — BLOCKED. PR 140 is OPEN, MERGEABLE, merge state UNSTABLE. CI run https://github.com/Fejjii/AlphaTrade-AI/actions/runs/36118686688 at 2026-09-25T09:55:49Z: `deployment-safety`, `frontend`, `docker-build`, and Vercel checks SUCCESS; `backend` still IN_PROGRESS. Do not merge until `backend` is SUCCESS.

4. **Current branch, commit, and phase** — Product tree remains `main` at `8591b8d`. This handoff is published on a handoff-only branch. No product PR.

5. **Last completed task** — Repository reconciliation and this checklist. No activation.

6. **Architecture summary** — Staging compute is `alphatrade-api-staging` (`/health`, `preDeployCommand: alembic upgrade head`) and `alphatrade-paper-worker-staging` (`python -m app.workers.paper_worker`). Documented API URL: `https://alphatrade-api-staging.onrender.com`. Worker has no public HTTP URL. Alembic head on this tree is `f1a2b3c4d5e6`. PR 140 adds no migration. Configured evidence stays `binance_usdm` with secondary `bybit_usdt_perpetual`. A Binance HTTP 418, 429, 401, 403, or 451 switches the whole observation to Bybit on a new connection epoch. Binance and Bybit prints are never combined. Watcher mints a Candidate only on genuine `confirmed_setup`. It does not create ActionEligibility, a canonical TradePlan, a fill, or a close. `CanonicalTradePlanCommand` is constructed only in tests. `ExecutionService.apply_paper_plan_fill` has no HTTP route. `POST /execution/paper-plan/close` refuses without a filled journal row (`missing_open_trade` / `incomplete_fill`). Telegram cannot execute or close.

7. **Files changed** — Product files: none. Published: `HANDOFF.md`, `CHANGELOG_SESSION.md` on the handoff branch only.

8. **Important code references** — `render.yaml` (PR 140 adds the two evidence keys on both services); `docs/controlled_paper_activation.md`; `docs/live_market_staging_activation.md` (PR 140); `scripts/validate-live-market-staging.sh` (after merge still requires active source `binance_usdm` and Watcher/Telegram flags false); `backend/src/app/api/routes/execution.py`; `backend/src/app/services/canonical_paper_execution.py`; `backend/src/app/workers/paper_worker.py`; `backend/src/app/workers/watcher_paper.py`.

9. **Tests and validation** — No test suite was run. This run did not deploy. Public GETs from this VM: `https://fapi.binance.com` returned HTTP 451 and `https://api.bybit.com` returned HTTP 403. That is this VM's egress, not the Frankfurt Render service. Do not treat it as a staging result.

10. **Deployment and provider status** — Not deployed. Not armed. `EXECUTION_MODE=paper`. `ENABLE_REAL_TRADING=false`. BloFin demo stays disabled. No exchange mutation.

11. **Security and trading safety status** — Unchanged. No secrets read or written. Paper isolation and `EXCHANGE_MODE=paper_internal` stay required for live evidence. Replay while the Watcher is armed fails boot. After PR 140, `PERPETUAL_EVIDENCE_SOURCE=replay` with secondary `bybit_usdt_perpetual` also fails boot. Rollback must set secondary to `none` in the same change as replay, after both Watcher flags are false.

12. **Decisions confirmed** — PR 140 body records Bybit failover as AT-ADR-068 and leaves Telegram at AT-ADR-067. This run did not add a decision.

13. **Known issues and blockers** —
- exact blocker: steps 10–15 cannot be completed by an operator. There is no HTTP or documented command that creates a canonical TradePlan from a Watcher Candidate, and no HTTP route that records the paper fill `POST /execution/paper-plan/close` requires.
- exact command that failed: none. The gap is absence of an operator command, found by reading routes and call sites.
- concise evidence: `CanonicalTradePlanCommand(` appears only in tests. `apply_paper_plan_fill(` is called from tests and `ExecutionService`, not from a route. Watcher `run_cycle` counts `candidate_ids` and does not call execution.
- completed: this checklist and handoff.
- unchanged: application code, Render, flags, database.
- risk of continuing: arming Watcher or Telegram cannot produce the journal close, statistics, learning record, or journal Telegram reply the checklist asks to verify. Inventing a fill or inserting `confirmed_setup` would falsify the loop.
- application code modified: no.
- uncommitted product work: no.
- exact human action required: do not merge-and-activate the full loop. Choose either to stop at disarmed evidence validation, or to authorize a separate change that exposes an operator-safe canonical plan and fill path without exchange mutation. Do not enable real trading.
- exact Cursor instruction after resolution: see section 17.

14. **Remaining prioritized tasks** — Human decision above. PR 140 `backend` CI must still reach SUCCESS before any merge. The checklist in section 16 is the operator procedure after that decision. Do not run it from this session.

15. **Recommended model** — A later implementation of the missing plan/fill path needs a model that can follow the existing canonical execution and paper-safety tests. No model can make the current HTTP surface close a canonical paper trade.

16. **Exact next instruction for ChatGPT** — Treat the activation runbook as BLOCKED at the first canonical paper trade. Steps 1–9 are specified and must not be started until a human accepts the stop at step 10. Preserve paper mode, real trading false, BloFin demo disabled, Binance primary, Bybit secondary, and staged Watcher/Telegram arming.

17. **Exact next Cursor prompt** — Do not deploy and do not arm. After a human authorizes it, add an operator path that creates one canonical paper plan from an existing Watcher Candidate and records one internal paper fill, then prove `POST /execution/paper-plan/close` writes Journal, statistics, learning, and a Telegram journal reply. Keep `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`, `EXCHANGE_MODE=paper_internal`, `BLOFIN_DEMO_ENABLED=false`, and no exchange mutation.

## Activation checklist (do not execute in this session)

Documented API: `https://alphatrade-api-staging.onrender.com`. If the Render URL differs, use the dashboard URL for service `alphatrade-api-staging` and stop if the service name differs. Worker service has no public URL.

Shared pins on both services for every step: `ENVIRONMENT=staging`, `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`, `EXCHANGE_MODE=paper_internal`, `BLOFIN_DEMO_ENABLED=false` (unset means the code default false), `PROVIDER_MODE=fallback`. Do not set `BINANCE_API_KEY`, `BINANCE_API_SECRET`, `BINANCE_SECRET_KEY`, `BINANCE_FUTURES_API_KEY`, `BINANCE_FUTURES_API_SECRET`, `FAPI_API_KEY`, or `FAPI_API_SECRET`. Do not set a Bybit credential. No order, withdrawal, transfer, or leverage call.

### 1. Merge PR 140

- Render service: none.
- Environment variables: none.
- Expected value: PR 140 head `1fd28ee1aedc936f1745324b88fba61a7b4c13f6` onto `main`, only after CI `backend` is SUCCESS. Record the merge SHA. That SHA is UNKNOWN until merge.
- Validation command: `gh pr view 140 --json state,mergeable,statusCheckRollup,mergeCommit`
- PASS: state MERGED, `backend` conclusion SUCCESS, merge commit recorded.
- STOP: `backend` not SUCCESS, merge conflicts, or any check failed. Do not merge from this runbook while `backend` is IN_PROGRESS.

### 2. Render Blueprint / environment changes

- Render services: `alphatrade-api-staging` and `alphatrade-paper-worker-staging` (Frankfurt, Docker, Starter).
- Set on both, from merged `render.yaml`: `PERPETUAL_EVIDENCE_SOURCE=binance_usdm`, `PERPETUAL_EVIDENCE_SECONDARY_SOURCE=bybit_usdt_perpetual`, `MARKET_DATA_FUTURES_BASE_URL=https://fapi.binance.com`, `BYBIT_PERPETUAL_BASE_URL=https://api.bybit.com`, `PERPETUAL_EVIDENCE_TIMEOUT_SECONDS=10`.
- Keep false on both: `MARKET_WATCHER_ENABLED`, `MARKET_WATCHER_BRIDGE_ENABLED`, `MARKET_WATCHER_BRIDGE_AUTO_TICK`, `WATCHER_ORCHESTRATION_ENABLED`, `WATCHER_PAPER_STAGING_ACTIVATION`, `TELEGRAM_ALERTS_ENABLED`, `TELEGRAM_INTERACTION_ENABLED`, `AUTOMATIC_TELEGRAM_DELIVERY_ENABLED`, `TELEGRAM_PAPER_ACTIVATION_ARMED`, `TELEGRAM_NETWORK_PERMITTED`. `TELEGRAM_INBOUND_MODE=off`. `ALERT_DELIVERY_ENABLED=false` and `ALERT_WEBHOOK_ENABLED=false` on the API. `BILLING_ENABLED=false`.
- Secrets stay dashboard-only and are not printed: `DATABASE_URL`, `REDIS_URL`, `QDRANT_URL`, `QDRANT_API_KEY`, `JWT_SECRET`, `OPENAI_API_KEY`. Confirm they exist as booleans. Do not change them in this step.
- Validation command: compare the dashboard keys to merged `render.yaml`. Do not restart onto replay with the Bybit secondary still set.
- PASS: both services show the five evidence values above and every arming flag false.
- STOP: primary is not `binance_usdm`, secondary is not `bybit_usdt_perpetual`, a Binance or Bybit secret is present, `BLOFIN_DEMO_ENABLED=true`, or `EXCHANGE_MODE` is not `paper_internal`.

### 3. API deployment

- Render service: `alphatrade-api-staging`.
- Command: deploy the merge commit. Image `backend/Dockerfile`, context `backend`, health check `/health`, pre-deploy `alembic upgrade head`.
- Environment: step 2 values. Arming flags stay false.
- Validation command: `curl -fsS https://alphatrade-api-staging.onrender.com/health` and `BASE_URL=https://alphatrade-api-staging.onrender.com ./scripts/verify-safety.sh`.
- PASS: HTTP 200; `environment=staging`; `execution_mode=paper`; `real_trading_enabled=false`; `exchange_mode=paper_internal`; `git_sha` equals the merge commit; `perpetual_evidence_source=binance_usdm`; `perpetual_evidence_secondary_source=bybit_usdt_perpetual`; `perpetual_evidence_activation=active`; `live_quote_freshness_seconds=10`; `first_perpetual_symbol=BTCUSDT`; `live_market_read_only=true`; `exchange_credentials_used_for_market_evidence=false`; `spot_fallback_permitted=false`; `fabricated_fallback_permitted=false`; Watcher and Telegram flags false. Release log shows Alembic at head `f1a2b3c4d5e6` (no new revision in PR 140).
- STOP: non-200, `activation=refused`, source `replay`, real trading true, mode not paper, `git_sha` mismatch, or Alembic failure. Roll back the API image. Do not downgrade Alembic.

### 4. Combined Paper Worker deployment

- Render service: `alphatrade-paper-worker-staging`.
- Command: `python -m app.workers.paper_worker` on the same merge commit. No `preDeployCommand`. Do not start `python -m app.workers.watcher_paper` or `python -m app.telegram_activation run`.
- Environment: same disarmed values as step 2. No `TELEGRAM_BOT_TOKEN`.
- Validation command: worker log event `paper_worker_boot` with `posture=disarmed`, `paper_only=true`, `enable_real_trading=false`, `real_trading_enabled=false`. API `GET /health` `worker_runtime.watcher.health_state` stays `UNAVAILABLE` or not `RUNNING`, and `worker_runtime.telegram.telegram_runtime_state` is not `enrollment` or `projection`.
- PASS: process stays up disarmed and does not scan.
- STOP: log `posture=armed`, any arming flag true, or the process opens exchange credentials.

### 5. Validate Binance/Bybit live BTCUSDT evidence

- Render service: `alphatrade-api-staging` (read). Worker stays disarmed.
- Environment: unchanged from step 2.
- Validation commands, Watcher and Telegram still off:
  - `BASE_URL=https://alphatrade-api-staging.onrender.com ./scripts/validate-live-market-staging.sh --remote --require-active`
  - Authenticated `GET /canonical/market-status?symbol=BTCUSDT`
  - From the Render region, not from an unrelated VM: `GET https://fapi.binance.com/fapi/v1/aggTrades?symbol=BTCUSDT&limit=1` and `GET https://api.bybit.com/v5/market/recent-trade?category=linear&symbol=BTCUSDT&limit=1`
- PASS: script exits 0 with source `binance_usdm` and activation `active`. Market status: `symbol=BTCUSDT`, `perpetual=true`, `live_executable=false`, `activation.state=active`, `activation.configured_source=binance_usdm`, `activation.trade_freshness_seconds=10`, `current_price.presentation=live_mark`, `current_price.is_live=true`, `current_price.fallback_used=false`, `source.provider_symbol=BTCUSDT`, `source.market_type=perpetual`, `source.is_mock=false`. Exactly one venue: `source.venue=binance` with `source.source_family=binance_usdm_futures_public`, or, only after a real Binance 418/429/401/403/451, `source.venue=bybit` with `source.source_family=bybit_usdt_perpetual_public`. Bybit body `retCode=0`, `result.category=linear`, symbol `BTCUSDT`. The script does not assert the secondary key; also require health `perpetual_evidence_secondary_source=bybit_usdt_perpetual`.
- STOP: `replay`, `refused`, stale, missing quote, spot, fabricated price, `fallback_used=true`, venue `blofin`, OKX, both venues in one payload, or the script failing because a Watcher/Telegram flag is true. Do not force a Binance ban.

### 6. Arm Watcher safely

- Render service: `alphatrade-paper-worker-staging` only. API flags stay false.
- Set on the worker only: `WATCHER_PAPER_STAGING_ACTIVATION=true`, `WATCHER_ORCHESTRATION_ENABLED=true`. Evidence keys stay as in step 2. `MARKET_WATCHER_ENABLED=false` and both bridge flags false. Telegram flags stay false. Restart only that worker.
- The armed process requires the existing secret booleans (`DATABASE_URL`, `REDIS_URL`, `JWT_SECRET`). Do not print them.
- Validation command: worker log does not contain `watcher_paper_activation_refused`. `GET /health` on the API: `execution_mode=paper`, `real_trading_enabled=false`, API `watcher_paper_staging_activation=false`, API `watcher_orchestration_enabled=false`, `worker_runtime.watcher.available=true`, `worker_runtime.watcher.health_state=RUNNING`, `worker_runtime.watcher.activation_state=running`, `worker_runtime.watcher.market_source=binance_usdm`, `worker_runtime.watcher.heartbeat_age_seconds` less than or equal to `90`, `worker_id` contains `watcher-paper-1` plus a 16-hex suffix.
- PASS: those health fields, and an approved or active compiled strategy already exists so preflight can scan.
- STOP: `watcher_paper_activation_refused`, `activation_state=refused`, API arm flags true, replay source, no approved compiled strategy, migration reason `migration_unhealthy`, or `real_trading_enabled=true`. Do not re-run `validate-live-market-staging.sh --remote --require-active` after this step; it requires the flags false. Use `./scripts/verify-safety.sh` instead.

### 7. Verify continuous Watcher scans

- Render service: same worker. Environment unchanged from step 6.
- Validation command: two `GET /health` reads at least 15 seconds apart (`watcher_paper_poll_interval_seconds` default 15).
- PASS: both reads have `worker_runtime.watcher.health_state=RUNNING`, `available=true`, heartbeat age `<= 90`, and `last_scan_at` advances. `last_scan_reason` is `completed` or `idle`. `kill_switch_active=false` unless a human intentionally left the switch on, in which case state may be `monitoring` and no Candidate is minted. Market status from step 5 still shows one live BTCUSDT venue. No order endpoint was called.
- STOP: `STALE`, `UNAVAILABLE`, reason `stale_evidence`, `provider_outage`, `wrong_source`, or `migration_unhealthy`. Go to rollback.

### 8. Telegram bot enrollment

- Render service: `alphatrade-paper-worker-staging` for secrets and polling. `alphatrade-api-staging` receives only `TELEGRAM_BOT_ID` (numeric bot user id, the digits before `:` in the token). Do not set `TELEGRAM_BOT_TOKEN` on the API.
- Worker values: `TELEGRAM_INTERACTION_ENABLED=true`, `TELEGRAM_NETWORK_PERMITTED=true`, `TELEGRAM_INBOUND_MODE=polling`, `TELEGRAM_PAPER_ACTIVATION_ARMED=false`, `TELEGRAM_BOT_ID` same numeric id, `TELEGRAM_BOT_TOKEN` secret, `TELEGRAM_CHAT_ID` empty, `TELEGRAM_ALERTS_ENABLED=false`, `AUTOMATIC_TELEGRAM_DELIVERY_ENABLED=false`, `TELEGRAM_WEBHOOK_SECRET` empty. Watcher flags stay true on the worker. Restart the worker once.
- Validation command: authenticated `POST /telegram-paper/enrollment/start`. Send the returned `token` once in a private chat with the bot. Do not log the token. Then `GET /health`.
- PASS: response has `challenge_id`, `expires_at`, `token`, `bot_id`. `worker_runtime.telegram.telegram_runtime_state=enrollment`, `health_state=RUNNING`, `available=true`, `inbound_mode=polling`. API `GET /health/telegram-paper-activation` `verdict=NOT_ARMED`. Watcher still `RUNNING`. Group chats do not bind.
- STOP: `bot_identity_mismatch`, token logged, webhook secret set, API holds the bot token, or Watcher heartbeat goes `STALE`.

### 9. Arm Telegram

- Render service: `alphatrade-paper-worker-staging` only.
- Set `TELEGRAM_PAPER_ACTIVATION_ARMED=true` and `TELEGRAM_CHAT_ID` to the verified private chat id. Keep polling, network, interaction, bot id, and bot token. Keep Watcher flags true. Keep alerts, automatic delivery, and webhook secret off. Restart the worker once.
- Validation command: `GET /health`.
- PASS: `worker_runtime.telegram.telegram_runtime_state=projection`, `activation_state=running`, `health_state=RUNNING`, `available=true`, `inbound_mode=polling`, `outbox_dead_letter=0`. Watcher still `RUNNING`. API process flags stay disarmed, so API `GET /health/telegram-paper-activation` stays `verdict=NOT_ARMED`. That is expected. Do not set the arm on the API to change it. `execution_mode=paper`, `real_trading_enabled=false`.
- STOP: API `telegram_paper_activation_armed=true`, worker state not `projection`, `last_error_code` `recipient_binding_missing`, or any live-trading flag.

### 10. Execute or observe the first canonical paper trade

- Render services: unchanged. This step is the blocker.
- What the worker does: scan to a genuine `confirmed_setup` and one Candidate. It does not create a plan, fill, or close.
- Validation command: authenticated `GET /canonical/candidates`, then `GET /canonical/setup-assessments/{assessment_id}`.
- Observe PASS, which is not a trade: an item with `authority=canonical`, `candidate.state=active`, and setup `assessment_state=confirmed_setup`, `live_executable=false`. Evidence venue is `binance` or `bybit`, matching step 5, symbol BTCUSDT. It was not inserted by hand.
- Trade PASS: not available. `POST /execution/paper-plan` exists but requires `account_id`, `authorization_id`, `revision_id`, and `idempotency_key` from a plan that no route can create.
- STOP: do not insert `confirmed_setup`. Do not call a guessed plan-create URL. Do not call `apply_paper_plan_fill` from a shell. Do not send Telegram text `execute paper` as a substitute. Stop the activation here until a human authorizes the missing operator path.

### 11. Close paper trade

- Render service: `alphatrade-api-staging`, authenticated trader `POST /execution/paper-plan/close`.
- Body fields that exist: `account_id`, `revision_id`, `command_id`, `exit_price` (positive), `fees`, `funding`, `slippage` (non-negative; default 0), `exit_reason` (1–60 chars), `occurred_at` (timezone-aware), `idempotency_key` (1–128 chars). Exit price is caller-supplied. The route does not read the market and does not call an exchange. Gross and net PnL are computed.
- PASS: `live_executable=false`, `replayed=false` on the first key, `journal_trade_id` present, `result` one of `win`, `loss`, `breakeven`.
- STOP: this command cannot pass until step 10 has a filled journal row. `missing_open_trade`, `incomplete_fill`, and `canonical_plan_required` are stops. A second different idempotency key after success returns `already_closed`. Do not invent a fill.

### 12. Verify Journal

- Command: authenticated `GET /journal/trades?source=paper_execution&status=closed&symbol=BTCUSDT` and `GET /journal/trades/{journal_trade_id}`.
- PASS: the close `journal_trade_id` has `source=paper_execution`, `status=closed`, `symbol=BTCUSDT`, `exit_reason` equal to the close body, `net_pnl` and `result` equal to the close response. Lifecycle source system is `canonical_paper_execution`.
- STOP: no such row, status not `closed`, or `result=open`.

### 13. Verify PnL / win-loss / statistics

- Command: authenticated `GET /journal/statistics?group_by=overall&symbol=BTCUSDT`.
- PASS: `overall.trade_count` includes that closed trade once; `wins`, `losses`, and `breakeven` match `result`; `net_pnl_total` includes that `net_pnl`; `truncated=false`. Replay of the same close key does not increase `trade_count`. Only closed trades are counted.
- STOP: open trades counted, a second row for the same close, or totals that do not match the close response.

### 14. Verify learning / evaluation

- Commands: authenticated `GET /canonical/learning/records/{candidate_id}`, `GET /canonical/learning/strategy-stats`, `GET /canonical/paper-evaluation/summary`.
- PASS: learning `authority=canonical`; `record.facts.strategy_pattern.closed=true`; `record.facts.outcome.result` and `net_pnl` match the close. Evaluation `summary.live_executable=false`, `summary.facts.watcher_orchestration_enabled=false`, `summary.facts.telegram_interaction_enabled=false`, and every refinement has `activate=false` and `auto_activate=false`.
- STOP: missing record, outcome disagrees with the journal row, or any refinement is activated.

### 15. Verify Telegram discussion

- Render service: the already armed paper worker. No new env.
- In the bound private chat, send a message containing `journal`, then one containing `learning`.
- PASS: journal reply starts with `Paper journal outcome (facts only, not a live fill).` and includes the `trade_id`, `result`, and `net_pnl`. Learning reply contains `Learning summary (explanation of recorded facts).` Neither reply contains `guaranteed`, `will win`, or `autonomous profit`. A message containing `execute paper` does not place an order.
- STOP: reply says `No journal outcome is bound for this tenant.` after a claimed close, the bot executes, or the reply claims a live fill.

### 16. Rollback if any gate fails

- Command that prints the plan and changes nothing: `./scripts/controlled-paper-activation-rollback.sh`. `--apply` exits 2 and changes nothing. Also `./scripts/watcher-paper-rollback.sh` prints only.
- Human order, `alphatrade-paper-worker-staging` then the API:
  1. SIGTERM the paper worker and confirm the process exited.
  2. `TELEGRAM_PAPER_ACTIVATION_ARMED=false`.
  3. `TELEGRAM_INBOUND_MODE=off`.
  4. Clear `TELEGRAM_WEBHOOK_SECRET` without logging it.
  5. `TELEGRAM_NETWORK_PERMITTED=false`.
  6. `TELEGRAM_INTERACTION_ENABLED=false`.
  7. Keep `TELEGRAM_ALERTS_ENABLED=false` and `AUTOMATIC_TELEGRAM_DELIVERY_ENABLED=false`.
  8. `WATCHER_PAPER_STAGING_ACTIVATION=false`.
  9. `WATCHER_ORCHESTRATION_ENABLED=false`.
  10. Keep `MARKET_WATCHER_ENABLED=false` and both bridge flags false.
  11. In one save, `PERPETUAL_EVIDENCE_SOURCE=replay` and `PERPETUAL_EVIDENCE_SECONDARY_SOURCE=none`. Replay while the Watcher is still armed fails boot. Replay while the secondary is still `bybit_usdt_perpetual` fails boot.
  12. Keep `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`, `EXCHANGE_MODE=paper_internal`, `BLOFIN_DEMO_ENABLED=false`.
  13. Do not add exchange credentials. Do not delete outbox, cursor, audit, Candidate, Journal, or lease rows. Do not downgrade Alembic. Do not clear the kill switch.
  14. Restart the API and the paper worker.
- PASS: `GET /health` shows `execution_mode=paper`, `real_trading_enabled=false`, `perpetual_evidence_source=replay`, `perpetual_evidence_secondary_source=none`, `perpetual_evidence_activation=inactive`. `GET /health/telegram-paper-activation` shows `verdict=NOT_ARMED`. Worker log `paper_worker_boot` `posture=disarmed`. `BASE_URL=https://alphatrade-api-staging.onrender.com ./scripts/validate-live-market-staging.sh --remote --expect inactive` exits 0.
- STOP: service will not boot, `real_trading_enabled=true`, or secondary still `bybit_usdt_perpetual` while source is `replay`.

## Blocker block

- exact blocker: canonical paper plan creation and paper fill have no operator command.
- exact failed command: none.
- error evidence: routes stop at Candidate reads plus `POST /execution/paper-plan` and `POST /execution/paper-plan/close`.
- completed: checklist and handoff. Not completed: merge, deploy, arm, trade, journal proof.
- unchanged: code and live environment.
- risk of continuing: a forced fill or hand-inserted setup would not be the canonical loop.
- application code modified: no.
- uncommitted product work: no.
- human action: do not activate. Authorize a follow-up only if the missing plan and fill path should be built.
- Cursor instruction after resolution: section 17.
