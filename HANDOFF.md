Status: REVIEW_REQUIRED
Last Updated: 2026-09-16T17:15:40+00:00
Task: AT-041 — Watcher orchestration foundation (isolated, disabled)
Current Phase: PARALLEL WATCHER WAVE — Agent 2 complete, awaiting integration review
Progress: Isolated orchestrator, tests, ruff, mypy, full backend, and GitHub CI complete
Blocker: None
Human Action Needed: Do not merge. Review draft PR #78 with the other two parallel watcher PRs, then plan PostgreSQL binding.
Next Step: Integration review of all three parallel PRs; bind typed watcher ports to PostgreSQL

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-16T17:15:40+00:00
Generated At UTC: 2026-09-16T17:15:40Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260916-171540
Task ID: AT-041
Current Branch: cursor/watcher-orchestration-foundation-4364
Current Commit: a75cdf3
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: d9c0291a07e03833efb3484e23d222e606d07f7e43a048dbf4f11f1f5d6b5846
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Agent 2 delivered an isolated watcher orchestration
   foundation on `cursor/watcher-orchestration-foundation-4364` (base
   `main@7ac1984675c698ee5b8ce19dcfd6cb1ec0e47bb2`). Draft PR
   https://github.com/Fejjii/AlphaTrade-AI/pull/78 HEAD
   `a75cdf39aaee8200761c71519b0faafafb65641f`. Watcher stays disabled. No
   automatic trading, candidates, Telegram, execution, journal, ORM models, or
   Alembic migrations. GitHub CI run 35124071670 success.

2. **Goal** — Build watcher worker architecture, scan/watchlist-policy contracts,
   leases, fencing, retry, scan lineage, dedupe, idempotent scheduling, manual
   and worker evaluation-boundary parity, health, failure reporting,
   observability, and deterministic test repositories — without market
   semantics or candidate logic.

3. **Current status** — `REVIEW_REQUIRED`. Implementation and validation complete.
   Do not merge. Next step is integration review with the other parallel PRs.

4. **Current branch, commit, and phase** —
   `cursor/watcher-orchestration-foundation-4364` @ `a75cdf3`. Phase: parallel
   watcher wave Agent 2 complete.

5. **Last completed task** — Isolated `app.watcher` package plus foundation tests
   and CI. Handoff published on `cursor/handoff-watcher-orch-4364` (no PR).

6. **Architecture summary** — New package `backend/src/app/watcher/` is isolated
   from `MarketWatcherService` and the live worker loop. Manual and worker
   callers share `WatcherOrchestrator.evaluate`. Persistence is a typed
   `WatcherStore` port with `InMemoryWatcherStore` for this wave. One fenced
   worker owns a scan scope; stale fence holders cannot publish. Every scan has
   an immutable lineage. Health is exclusive `healthy | degraded | blocked |
   stale`. Failures cannot be rewritten as successes. Source freshness and
   evidence validity are opaque tokens only (Agent 1). `PERSIST_AND_NOTIFY` is
   blocked. `WATCHER_ORCHESTRATION_ENABLED` defaults false.

7. **Files changed** —
   - `backend/src/app/watcher/*` (new isolated package)
   - `backend/tests/test_watcher_orchestration_foundation.py` (26 tests)
   - `backend/src/app/core/config.py` (disabled orchestration flags)
   - env templates + `render.yaml` (`WATCHER_ORCHESTRATION_ENABLED=false`)
   - `.ai/DECISIONS.md` AT-ADR-021; `.ai/TASKS.md` AT-041
   - staging docs flag tables

8. **Important code references** —
   - `WatcherOrchestrator.evaluate` / `run_worker` / `evaluate_manual`
   - `InMemoryWatcherStore.claim_lease` / `fence_is_active`
   - `project_health`
   - `runtime_config_from_settings`

9. **Tests and validation** —
   - Targeted: `uv run pytest tests/test_watcher_orchestration_foundation.py`
     26 passed, exit 0
   - Local full backend: `uv run pytest` 1641 passed, 47 skipped, exit 0
     (0:16:35). The 47 skips are Postgres-gated tests without local
     `AT028_POSTGRES_URL`.
   - Ruff check/format: pass (full backend `.`)
   - `uv run mypy --strict src/app/watcher`: Success, 11 files
   - GitHub CI: https://github.com/Fejjii/AlphaTrade-AI/actions/runs/35124071670
     success. Backend job 1688 passed (skips executed against Postgres 16).
     deployment-safety, frontend, evaluation, docker-build, e2e-smoke success.

10. **Deployment and provider status** — No deploy. Staging flags unchanged
    except documenting `WATCHER_ORCHESTRATION_ENABLED=false`.

11. **Security and trading safety status** — `EXECUTION_MODE=paper`,
    `ENABLE_REAL_TRADING=false`, `WATCHER_ORCHESTRATION_ENABLED=false`,
    `MARKET_WATCHER_ENABLED=false`. Live trading disabled. No execution/journal
    /Telegram side effects from this package.

12. **Decisions confirmed** — AT-ADR-021: typed persistence ports now; PostgreSQL
    later; watcher remains disabled; notify/candidate creation forbidden.

13. **Known issues and blockers** — None for this slice. Agent 1 freshness and
    Agent 3 (if any) market/candidate work are out of scope. PostgreSQL binding
    is intentionally not in this PR.

14. **Remaining prioritized tasks** — Integration review of the three parallel
    watcher PRs; bind `WatcherStore` to PostgreSQL; compose Agent 1 source
    contracts; keep flags off.

15. **Recommended model** — Cursor Grok 4.6 or Opus 4.8 for integration review.

16. **Exact next instruction for ChatGPT** — Collect the three parallel watcher
    PRs. Do not merge PR #78 until integration review. Plan PostgreSQL binding
    of `app.watcher` ports after review.

17. **Exact next Cursor prompt** — After human approval of the three parallel
    PRs, implement the PostgreSQL adapter for `WatcherStore` (leases, lineage,
    schedules, heartbeats) with concurrency tests against real Postgres. Do not
    enable `WATCHER_ORCHESTRATION_ENABLED`. Do not add market adapters,
    candidates, Telegram, execution, or journal side effects.

## Review-required extras (§13.5)

- Exact blocker: None (review pause, not a technical failure)
- Exact command or action that failed: None
- Concise error output: None
- Already completed: isolated `app.watcher`, 26 foundation tests, ruff, mypy,
  local full backend, GitHub CI, draft PR #78
- Remains unchanged: shared ORM models, Alembic migrations, live worker wiring,
  market adapters, CVD, pattern evaluation, candidate lifecycle, Telegram,
  execution, BloFin, journal automation, frontend
- Risk of continuing: merging before the other parallel PRs are reviewed would
  bind incomplete contracts; do not enable the watcher flag
- Application code modified: yes, isolated watcher package plus disabled flags
- Uncommitted product work: none (HEAD `a75cdf3`, product tree CLEAN)
- Exact human action required: review draft PR #78 with the other two parallel
  watcher PRs; do not merge
- Exact Cursor instruction after resolution: bind `WatcherStore` to PostgreSQL
  without enabling orchestration

WATCHER CORE PASS
FENCING PASS
LEASES PASS
IDEMPOTENCY PASS
SCAN LINEAGE PASS
WORKER HEALTH PASS
MANUAL WORKER PARITY PASS
WATCHER ENABLED NO
DATABASE MIGRATION NONE
LIVE TRADING DISABLED
FULL BACKEND 1641 passed / 47 skipped (local); CI backend 1688 passed
CI success https://github.com/Fejjii/AlphaTrade-AI/actions/runs/35124071670
BLOCKERS None
NEXT integration review
