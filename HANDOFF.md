Status: REVIEW_REQUIRED
Last Updated: 2026-09-17T11:29:20+00:00
Task: AT-045 — Phase 6 candidate lifecycle service
Current Phase: Phase 6 candidate authority (in-memory)
Progress: Draft PR #86 open; local and GitHub CI green on HEAD
Blocker: None
Human Action Needed: Review draft PR https://github.com/Fejjii/AlphaTrade-AI/pull/86. Do not merge.
Next Step: Independent review of Candidate authority; keep PostgreSQL/Alembic and fusion evaluator on separate agents

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-17T11:29:20+00:00
Generated At UTC: 2026-09-17T11:29:20Z
Timezone: UTC
Session ID: AT-SESSION-20260917-102600
Task ID: AT-045
Current Branch: cursor/phase6-candidate-lifecycle-service-1c4d
Current Commit: d109459
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: d16d3f655d3f4dcacf44257bc30d31fa16a31643a33c0f85b1b225a2669955cd
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Canonical Phase 6 candidate lifecycle application service is implemented on `cursor/phase6-candidate-lifecycle-service-1c4d` against frozen PR 84 HEAD `5db47b728ba255b993717f59922076733630d510`. Draft PR #86. Candidate authority only; no fusion evaluator, PostgreSQL, or Alembic.

2. **Goal** — Idempotent candidate creation from `CONFIRMED_SETUP` + exact `CanonicalEvidenceWindowV1` + tenant-owned `CompiledSetupDefinition`; uniqueness via frozen `CandidateUniquenessTuple`; append-only transitions; terminal non-resurrection; tenant isolation; `PaperValidationCandidate` blocked as authority.

3. **Current status** — REVIEW_REQUIRED. Implementation complete. Draft PR open. Do not merge.

4. **Current branch, commit, and phase** — Branch `cursor/phase6-candidate-lifecycle-service-1c4d`. HEAD `d109459` (TASKS validation note). Implementation commit `e43176b`. Base `5db47b7` (PR 84). Phase 6 candidate lifecycle.

5. **Last completed task** — AT-045 in-memory candidate lifecycle service, ports, tests, local validation, GitHub CI on `e43176b`.

6. **Architecture summary** — `CandidateLifecycleService` is the only mint path. Repository port + `InMemoryCandidateRepository` (RLock, uniqueness hash index, append-only history). Deterministic UUID5 candidate IDs from the uniqueness hash. No second source of truth.

7. **Files changed** — `backend/src/app/signal_fusion/{lifecycle,ports,memory,errors,__init__}.py`; `backend/tests/test_phase6_candidate_lifecycle.py`; `backend/tests/support/phase6_fusion.py`; `.ai/DECISIONS.md` (AT-ADR-025); `.ai/TASKS.md` (AT-045).

8. **Important code references** — `CandidateLifecycleService.create_from_confirmed_setup`; `create_from_paper_validation_candidate` always raises `LegacyCandidateAuthorityError`; `InMemoryCandidateRepository.get_or_insert_created` / `commit_transition`.

9. **Tests and validation** —
   - `uv run pytest tests/test_phase6_candidate_lifecycle.py` — 29 passed, exit 0
   - Phase 6 contract tests — 56 passed (34 + 22), exit 0
   - Local full backend pytest — exit 0; 1843 passed, 47 skipped (no Postgres)
   - `uv run ruff check .` / `uv run ruff format --check .` — pass
   - `uv run mypy --strict src/app/signal_fusion` — Success, 14 files
   - GitHub CI on `e43176b`: run 35212249349 success; backend 1890 passed; ruff; frontend; docker-build; deployment-safety; evaluation; e2e-smoke
   - GitHub CI on HEAD `d109459`: run 35214102528 success; backend 1890 passed; ruff; frontend; docker-build; deployment-safety; evaluation; e2e-smoke

10. **Deployment and provider status** — Unchanged. No deploy.

11. **Security and trading safety status** — Paper only. `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`. No network in this service. No execution. No feature activation.

12. **Decisions confirmed** — AT-ADR-025: Candidate authority is this service; uniqueness is the frozen tuple; PVC cannot mint; in-memory port only in this slice.

13. **Known issues and blockers** — None. Draft-PR merge gate is intentional.

14. **Remaining prioritized tasks** — PostgreSQL candidate repository + Alembic (separate agent). Fusion evaluator (separate agent). Do not merge #86 until review.

15. **Recommended model** — Cursor Grok 4.6

16. **Exact next instruction for ChatGPT** — AT-045 lifecycle service is in draft PR #86 stacked on PR 84. Do not merge. Next parallel work is durable PostgreSQL binding to `CandidateRepository` without changing uniqueness or lifecycle rules, or fusion evaluator, not both in one agent.

17. **Exact next Cursor prompt** — Implement the Phase 6 PostgreSQL `CandidateRepository` adapter and Alembic migration against the frozen `CandidateUniquenessTuple` and `CandidateLifecycleService` port. Do not change lifecycle rules. Do not implement the fusion evaluator. Paper only. Draft PR. Do not merge.
