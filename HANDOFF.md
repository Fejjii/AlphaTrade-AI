Status: REVIEW_REQUIRED
Last Updated: 2026-09-16T19:47:22+00:00
Task: AT-041 — Watcher tenant isolation hardening (PR 78)
Current Phase: PARALLEL WATCHER WAVE — Agent 2 tenant isolation complete, awaiting integration review
Progress: Structural tenant keys, two-org isolation tests, ruff, mypy, full backend, and GitHub CI complete
Blocker: None
Human Action Needed: Do not merge. Review draft PR #78 with the other two parallel watcher PRs, then plan PostgreSQL binding with tenant keys preserved.
Next Step: Final integration review of all three parallel PRs; bind typed watcher ports to PostgreSQL using (organization_id, scan_scope)

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-16T19:47:22+00:00
Generated At UTC: 2026-09-16T19:47:22Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260916-194722
Task ID: AT-041
Current Branch: cursor/watcher-orchestration-foundation-4364
Current Commit: 380cf2f
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: 7772f144d4fbf283335c388a139c671235850232e0cc72f9bdcdac18a6d4dd52
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Tenant isolation hardening is on draft PR
   https://github.com/Fejjii/AlphaTrade-AI/pull/78 HEAD
   `380cf2f478c581a3aeba8bbc03e106592476db65`. Watcher synchronization state is
   keyed by `(organization_id, scan_scope)`. Two organizations with the exact
   same `scan_scope` independently acquire leases; one tenant cannot invalidate
   the other tenant fence. Watcher stays disabled. No migrations. GitHub CI run
   35140244497 success.

2. **Goal** — Resolve the independent review finding: do not rely on embedding
   organization ID inside `scan_scope` strings. Pass `organization_id` through
   WatcherStore and reject mismatch.

3. **Current status** — `REVIEW_REQUIRED`. Implementation and validation complete.
   Do not merge.

4. **Current branch, commit, and phase** —
   `cursor/watcher-orchestration-foundation-4364` @ `380cf2f`. Phase: parallel
   watcher wave Agent 2 tenant isolation complete.

5. **Last completed task** — Structural tenant keys plus two-org concurrency
   tests and CI.

6. **Architecture summary** — `WatcherStore` lease, fence, heartbeat, health,
   latest-attempt-by-scope, and lineage-by-scope maps use
   `tenant_scope_key(organization_id, scan_scope)`. `derive_scan_scope` no longer
   embeds organization ID. `get_lineage` / `cas_lineage_terminal` require
   `organization_id` and raise `WatcherTenantMismatchError` on mismatch.
   Manual and worker callers still share `WatcherOrchestrator.evaluate`.
   `WATCHER_ORCHESTRATION_ENABLED` defaults false.

7. **Files changed** (this slice vs reviewed HEAD `a75cdf3`) —
   - `backend/src/app/watcher/ports.py`, `memory.py`, `orchestrator.py`,
     `contracts.py`, `health.py`, `hashing.py`, `errors.py`, `observability.py`
   - `backend/tests/test_watcher_orchestration_foundation.py` (31 tests)
   - `.ai/DECISIONS.md` AT-ADR-021 point 7; `.ai/TASKS.md` AT-041 validation

8. **Important code references** —
   - `tenant_scope_key`
   - `InMemoryWatcherStore.claim_lease` / `fence_is_active` / `latest_attempt_for_scope`
   - `WatcherOrchestrator.health(organization_id, scan_scope)`
   - `WatcherTenantMismatchError`

9. **Tests and validation** —
   - Targeted: `uv run pytest tests/test_watcher_orchestration_foundation.py`
     31 passed, exit 0
   - Local full backend: `uv run pytest` 1646 passed, 47 skipped, exit 0
     (0:16:53)
   - Ruff check/format: pass (full backend `.`)
   - `uv run mypy --strict src/app/watcher`: Success, 11 files
   - GitHub CI: https://github.com/Fejjii/AlphaTrade-AI/actions/runs/35140244497
     success. Backend job 1693 passed. deployment-safety, frontend, evaluation,
     docker-build, e2e-smoke, Vercel pass.

10. **Deployment and provider status** — No deploy.

11. **Security and trading safety status** — `EXECUTION_MODE=paper`,
    `ENABLE_REAL_TRADING=false`, `WATCHER_ORCHESTRATION_ENABLED=false`,
    `MARKET_WATCHER_ENABLED=false`. Live trading disabled. No execution/journal
    /Telegram side effects. No Alembic migrations.

12. **Decisions confirmed** — AT-ADR-021: tenant key is structural
    `(organization_id, scan_scope)`; typed ports now; PostgreSQL later; watcher
    remains disabled.

13. **Known issues and blockers** — None for this slice.

14. **Remaining prioritized tasks** — Final integration review of the three
    parallel watcher PRs; bind `WatcherStore` to PostgreSQL preserving tenant
    keys; compose Agent 1 source contracts; keep flags off.

15. **Recommended model** — Cursor Grok 4.6 or Opus 4.8 for integration review.

16. **Exact next instruction for ChatGPT** — Collect the three parallel watcher
    PRs. Do not merge PR #78. Postgres adapter must key leases/fences/health by
    `(organization_id, scan_scope)`.

17. **Exact next Cursor prompt** — After human approval of the three parallel
    PRs, implement the PostgreSQL adapter for `WatcherStore` with composite
    tenant keys and the same two-org same-scope concurrency tests against real
    Postgres. Do not enable `WATCHER_ORCHESTRATION_ENABLED`.

## Review-required extras (§13.5)

- Exact blocker: None (review pause, not a technical failure)
- Exact command or action that failed: None
- Concise error output: None
- Already completed: tenant-scoped store keys, mismatch rejection, 31 foundation
  tests, ruff, mypy, local full backend, GitHub CI, draft PR #78 updated
- Remains unchanged: shared ORM models, Alembic migrations, live worker wiring,
  market adapters, CVD, pattern evaluation, candidate lifecycle, Telegram,
  execution, BloFin, journal automation, frontend
- Risk of continuing: merging before the other parallel PRs are reviewed would
  bind incomplete contracts; do not enable the watcher flag
- Application code modified: yes, isolated watcher package tenant keys
- Uncommitted product work: none (HEAD `380cf2f`, product tree CLEAN aside from
  gitignored operational handoff copies)
- Exact human action required: review draft PR #78 with the other two parallel
  watcher PRs; do not merge
- Exact Cursor instruction after resolution: bind `WatcherStore` to PostgreSQL
  with `(organization_id, scan_scope)` keys without enabling orchestration

TENANT LEASE ISOLATION PASS
TENANT FENCING PASS
TENANT HEALTH ISOLATION PASS
TENANT LINEAGE ISOLATION PASS
WATCHER ENABLED NO
DATABASE MIGRATION NONE
LIVE TRADING DISABLED
FULL BACKEND 1646 passed / 47 skipped (local); CI backend 1693 passed
CI success https://github.com/Fejjii/AlphaTrade-AI/actions/runs/35140244497
BLOCKERS None
NEXT FINAL INTEGRATION REVIEW
