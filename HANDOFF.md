Status: READY
Last Updated: 2026-09-24T10:26:11+00:00
Task: BloFin Demo readiness audit (read-only)
Current Phase: AUDIT COMPLETE
Progress: Read-only audit of BloFin demo execution readiness on main@e5ba5d3. No code, deploy, or implementation commit.
Blocker: None
Human Action Needed: None
Next Step: Do not start BloFin demo loop wiring until the separate credential-isolation work is finished. Then implement the foundation slice in the plan (transport contract, send-once, NET gate, margin fields) before any venue order.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-24T10:26:11+00:00
Generated At UTC: 2026-09-24T10:26:11Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260924-102611
Task ID: BLOFIN-DEMO-READINESS-AUDIT
Current Branch: main
Current Commit: e5ba5d3
Working Tree Status: DIRTY
Generated By: Cursor
Handoff Status: READY
Source File SHA256: 9004519a30cf29e746e0c4b008617c130d8c6dc1b6dce646374090a1b3d94fce
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — BloFin Demo readiness is about 40%. A signed demo REST client, read probes, and a disconnected order provider exist. The current execution authority (`EXECUTE_PAPER_PLAN`) refuses any BloFin or network send. Real trading stays disabled. Verdict: FOUNDATION WORK REQUIRED.

2. **Goal** — Read-only readiness audit for BloFin Demo as the external demo execution venue. No code changes, no implementation commits, no deploy, no overlap with credential isolation.

3. **Current status** — READY (audit complete). Integration is not ready.

4. **Current branch, commit, and phase** — `main` @ `e5ba5d3`. Phase: audit only. Working tree dirty only if handoff files are present; they are gitignored for product commits.

5. **Last completed task** — Mapped client, account, execution, safety, journal, portfolio/risk, and current BloFin REST docs (docs.blofin.com, fetched 2026-09-24).

6. **Architecture summary** — Demo host allowlist is `demo-trading-openapi.blofin.com`. Production hosts are denied. `trade_live` refuses startup. `paper_exchange_demo` is staging-only. Credentials are process-wide env vars. `VenueSubmitDispatcher.attempt_fake_send` accepts only `FakeVenueSubmitProvider`. Legacy demo mirror raises `legacy_demo_mirror_disabled`.

7. **Files changed** — `HANDOFF.md`, `CHANGELOG_SESSION.md` only. No application code.

8. **Important code references** — `backend/src/app/providers/exchange/blofin_client.py`, `blofin_account.py`, `blofin_execution.py`, `factory.py`, `core/exchange_safety.py`, `services/execution_service.py`, `services/venue_submit_dispatcher.py`, `services/blofin_sync_service.py`.

9. **Tests and validation** — No tests executed. This was a read-only audit. Existing suites: `test_blofin_provider.py`, `test_blofin_execution.py`, `test_exchange_safety.py`, `test_at037_tradingview_blofin.py`. They mock HTTP and do not prove a live demo loop.

10. **Deployment and provider status** — `render.yaml` sets `EXCHANGE_MODE=paper_internal` and `ENABLE_REAL_TRADING=false`. BloFin credentials are not in the blueprint. Live staging was not queried.

11. **Security and trading safety status** — Real trading remains disabled. No orders placed. Audit did not read or print secrets. Credential-isolation implementation was not touched. BloFin client still reads `BLOFIN_API_KEY` / `SECRET` / `PASSPHRASE` from settings only.

12. **Decisions confirmed** — Do not revive the legacy demo mirror. Do not call `set-leverage` or `set-position-mode` in a first slice. Do not add a second secret store. Entry orders must not silently attach TP/SL. NET mode is the target position mode.

13. **Known issues and blockers** — Official order-detail path is `GET /api/v1/trade/order-detail`; code still calls `GET /api/v1/trade/order`. Leverage read uses deprecated `leverage-info`. Fills are a separate `fills-history` endpoint. Client retries POST on 429/5xx. Permission probe fails open and documented `query-apikey` does not expose withdraw scope. Docs (`docs/limitations_roadmap.md`, phase-0 audit) still describe a best-effort mirror that the code has disabled.

14. **Remaining prioritized tasks** — (1) Finish credential isolation elsewhere. (2) Foundation: order-detail, fills-history, availableEquity, send-once, NET gate. (3) Wire demo send through the execution protocol. (4) Reconcile fills, fees, positions into the journal. (5) Portfolio sizing from demo available equity.

15. **Recommended model** — Grok 4.7 or equivalent for the foundation slice; safety review before any staging order.

16. **Exact next instruction for ChatGPT** — Treat BloFin Demo as not loop-ready. Keep real trading off. Wait for credential isolation before implementation.

17. **Exact next Cursor prompt** — After credential isolation has landed, implement only the BloFin demo foundation slice: align REST paths with current docs (`order-detail`, `fills-history`, futures `availableEquity`), stop retrying ambiguous POSTs, fail closed on permission-probe failure, and require `net_mode`. Do not place orders, do not set leverage, do not attach TP/SL, do not add a secret store, and do not revive `legacy_demo_mirror`.
