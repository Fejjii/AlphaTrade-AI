Status: REVIEW_REQUIRED
Last Updated: 2026-09-22T19:49:09+00:00
Task: AT-080-AUDIT — Final independent activation audit of PR #131 (070169c), review only
Current Phase: AUDIT COMPLETE — verdict REMEDIATION REQUIRED; awaiting human decision
Progress: Full product loop traced (Binance USD-M → Watcher → Candidate → paper → Journal → evaluation → learning; Watcher → outbox → Telegram); 3 P0, 5 P1, 12 P2 findings with repository evidence; no code, env, or deploy changes
Blocker: None (review-only task; no technical blocker for the audit itself)
Human Action Needed: Read the audit report; do NOT merge, deploy, or set any activation flag until the P0/P1 items are remediated and re-audited
Next Step: Human decides remediation scope; then a remediation task on PR #131 (see section 17)

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-22T19:49:09+00:00
Generated At UTC: 2026-09-22T19:49:09Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260922-194909
Task ID: AT-080-AUDIT
Current Branch: main (review of origin/cursor/controlled_paper_activation_integration @ 070169c in a detached worktree)
Current Commit: 894e9e4
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: a668ee478aed19b61644f3e557e7d0986692ebe605a5999e1f52efdf97be1a79
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Review-only audit of PR #131 exact HEAD
   `070169cd92f1258be337f6fe5ef6230368768d5a`. Verdict: **REMEDIATION REQUIRED**.
   The trading-safety core holds (paper only, no exchange mutation, single Candidate
   authority behind Postgres fencing, Telegram/AI cannot mint, trade, approve, or enable
   live). The system is not operable end-to-end as the runbook describes: the Telegram arc
   has no production runtime (no outbox drain, no inbound poll loop, no webhook mount, no
   binding enrollment path); the Telegram bot token would be written to INFO logs by
   httpx once `TELEGRAM_NETWORK_PERMITTED=true`; and the live evidence assembly fetches an
   8-hour BTCUSDT aggTrades window per target per scan (twice), with no throttle, against a
   30-second lease TTL and an untested Binance weight budget. No code, environment, or
   deployment was changed. Nothing was activated.

2. **Goal** — Determine whether AlphaTrade is genuinely ready to activate the automated
   paper system against real read-only Binance USD-M market data, per the user's audit
   charter (architecture, authority, market data, Watcher, paper execution, risk, kill
   switch, tenancy, concurrency, leases, recovery, idempotency, persistence, migrations,
   Telegram, learning, AI boundaries, observability, deployment, rollback, secrets, failure
   cases, test quality).

3. **Current status** — Audit complete. Report delivered in the agent's final response.
   PR #131 CI is green (backend, deployment-safety, docker-build, frontend, e2e-smoke,
   evaluation). No live Binance run is recorded anywhere in the repository (UNKNOWN).

4. **Current branch, commit, and phase** — Workspace on `main @ 894e9e4` (clean). PR #131
   branch inspected read-only at `/tmp/pr131` (detached worktree at `070169c`). Phase: audit
   complete, review required.

5. **Last completed task** — AT-080 (PR #131) authored the controlled paper activation
   package. This session audited it.

6. **Architecture summary** — Binance USD-M public GET → `FirstSliceEvidenceAssembler` →
   `AssemblingWatcherScanEvidence` (monitor gate) → `WatcherPaperRuntime` → `WatcherOrchestrator`
   (Postgres lease + fencing) → `WatcherFusionEvaluationService.evaluate_canonical_strategy` →
   `SetupAssessment` → `CandidateLifecycleService.create_from_confirmed_setup` (fence-bound
   insert) → human API: eligibility → TradePlan → paper execution → Journal → attribution →
   evaluation → refinement (`activate=false`). Parallel: scan hook →
   `TelegramPaperAgent.project_watcher_notice` → outbox (no drain loop in production).

7. **Files changed** — None in the repository. Only gitignored `HANDOFF.md` and
   `CHANGELOG_SESSION.md` regenerated. A detached worktree was created outside the repo at
   `/tmp/pr131` for read-only inspection.

8. **Important code references** —
   `backend/src/app/controlled_activation/projection.py` (147–189 network transport / update
   source return None when not permitted; 99–102 raises; 138 `controller.arm()`);
   `backend/src/app/telegram_activation/controller.py` (137–166 `deliver`/`poll_once` have no
   production caller); `backend/src/app/telegram_activation/transport.py` (229–233 token in
   URL) + `backend/src/app/core/logging.py` (32 root INFO) + httpx 0.28.1 `_client.py:1025`;
   `backend/src/app/evidence_pipeline/assembler.py` (172–186 8h trade window) +
   `market_contracts/cvd.py:41` + `adapters/aggtrades.py` (27, 31, 162–215);
   `backend/src/app/watcher/orchestrator.py` (362–369, 474–505) +
   `persistence/watcher_postgres.py` (757–771) + `core/config.py:193` (TTL 30 s);
   `backend/src/app/workers/watcher_activation.py` (653–692 gate, 759–774 fresh monitor per
   cycle, 791–826 string-introspection pins); `backend/src/app/workers/watcher_paper.py`
   (402–435 stop on refusal, 597–614 kill switch fail-open reporting); `render.yaml:119–126`
   (worker runs `app.workers.entrypoint`, not `watcher_paper`).

9. **Tests and validation** — No tests were run by this session (review only). PR #131 CI
   run 35760545515 on `070169c`: all checks pass. AT-080 recorded `uv run pytest -q`
   2613 passed / 0 failed / 0 skipped. Test-quality findings are in the report (vacuous
   string-introspection pins, operator-authored smoke JSON, rehearsal test injects
   transport/update source/clock and calls `controller.deliver()` manually, kill-switch test
   encodes minting while the switch is active).

10. **Deployment and provider status** — `render.yaml` defines an API (web) and a worker
    running `python -m app.workers.entrypoint` with `WORKER_ENABLED=false`. No service runs
    `python -m app.workers.watcher_paper`. No Telegram variables in `render.yaml`. Live
    Binance evidence values are present as a blueprint only. Nothing deployed by this session.

11. **Security and trading safety status** — `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`,
    `EXCHANGE_MODE=paper_internal` pinned and validated; no exchange credentials; GET-only
    allowlisted HTTPS client. Findings: Telegram bot token leak into INFO logs via httpx
    request logging (P0); kill switch does not block scans/Candidate minting/Telegram
    projection despite docs (P1). No secrets were printed or committed by this session.

12. **Decisions confirmed** — None added to `.ai/DECISIONS.md` (review only). Recommended
    decisions for the human are listed in the report.

13. **Known issues and blockers** — See the final report: P0 ×3 (Telegram runtime absent,
    token log leak, Binance request budget/latency vs lease TTL untested), P1 ×5 (no
    deployment vehicle + brittle gate, step-7 runbook contradiction, kill-switch claim
    false, observability/health truthfulness, webhook unmountable), P2 ×12.

14. **Remaining prioritized tasks** — (1) Add an outbox drain + inbound poll loop to the
    cleared Watcher runtime or a dedicated process; add a production binding enrollment path.
    (2) Silence/redact `httpx`/`httpcore` stdlib loggers or route them through the redaction
    processor. (3) Measure and budget Binance request weight; heartbeat during evaluation or
    raise lease TTL; cache assembled evidence between `evaluate` and `persist`. (4) Define
    the dedicated Watcher worker in `render.yaml`; make the runtime gate tolerate transient
    failures with backoff. (5) Fix runbook step 7 / kill-switch / health claims.

15. **Recommended model** — Any current frontier model with strong Python static reasoning.

16. **Exact next instruction for ChatGPT** — "Read the PR #131 audit report (verdict
    REMEDIATION REQUIRED). Decide whether to (a) remediate P0-1..P0-3 and P1-1..P1-5 on the
    PR branch, or (b) descope the Telegram arm and activate only steps 1–4 (live evidence on
    the API) after measuring Binance request weight. Do not merge or deploy before that."

17. **Exact next Cursor prompt** — "On branch `cursor/controlled_paper_activation_integration`
    (base 070169c), remediate the P0/P1 findings from the AT-080 audit: (1) wire
    `TelegramPaperActivation.deliver()` and `poll_once()` into the cleared paper worker cycle
    (or a dedicated `python -m app.telegram_activation run` process) with a Postgres-backed
    lease, and add an authenticated enrollment API for the private binding; (2) set the
    `httpx` and `httpcore` stdlib loggers to WARNING in `configure_logging` and add a test
    that the bot token never appears in captured logs; (3) add a request-weight budget and
    Retry-After backoff to `ReadOnlyHttpGetClient`, heartbeat/renew the lease during
    `evaluate`, and reuse the assembled evidence between `evaluate` and
    `persist_confirmed_setup`; (4) add a `watcher-paper-staging` worker service to
    `render.yaml` running `python -m app.workers.watcher_paper`, and make the runtime gate
    back off on transient probe failures instead of exiting; (5) correct
    `docs/controlled_paper_activation.md` step 7, the kill-switch claims, and the health
    verification steps. Paper only. No deploy. No env edits."

<!-- REVIEW_REQUIRED details (§13.5):
- exact blocker: none for the audit; activation is blocked by the P0 findings in the report
- exact command or action that failed: none (review only); a read-only public probe of
  fapi.binance.com from this VM returned HTTP 451 (region), so live aggTrade volume is UNKNOWN
- what was already completed: full audit and report
- what remains unchanged: all repository code, env, deploy, PR #131 state
- risk of continuing: activating per the current runbook would leave the Watcher down between
  steps 7–8, never deliver Telegram messages, and could log the bot token
- whether application code was modified: NO
- whether uncommitted work exists: NO (only gitignored handoff files)
- exact human action required: decide remediation scope; do not merge/deploy/activate
- exact Cursor instruction after resolution: see section 17
-->
