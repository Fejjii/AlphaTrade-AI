Status: REVIEW_REQUIRED
Last Updated: 2026-09-20T15:56:38+00:00
Task: AT-STG-GATE3 — Fix canonical smoke step 10 HTTP 500
Current Phase: Draft PR open; CI green; do not merge or deploy
Progress: Root cause fixed; regression added; GitHub CI success
Blocker: None
Human Action Needed: Review draft PR #106; merge only after explicit approval; then redeploy staging so Alembic applies e8f1c4a9b702 and re-run INCLUDE_CANONICAL smoke
Next Step: Human review of https://github.com/Fejjii/AlphaTrade-AI/pull/106 — do not merge or deploy from this agent

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-20T15:56:38+00:00
Generated At UTC: 2026-09-20T15:56:38Z
Timezone: UTC
Session ID: AT-SESSION-20260920-151330
Task ID: AT-STG-GATE3
Current Branch: cursor/staging_gate3_500_fix
Current Commit: a555b5a
Working Tree Status: DIRTY
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: cd3d252548547b696156fb85b245df50ddb7cae2518e31be05b746eddfb55ca0
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Canonical smoke step 10 failed on deployed staging because `GET /journal/trades` selected ORM column `journal_trades.account_id` that Alembic never created. Empty synthetic tenants therefore 500'd. Strategy stats were healthy. Fix is Alembic `e8f1c4a9b702` plus an Alembic-schema empty-tenant regression. Draft PR #106; GitHub CI green. Not merged, not deployed.
2. **Goal** — Diagnose and fix the real staging 500 without redesign and without enabling Watcher, Telegram, or live trading.
3. **Current status** — REVIEW_REQUIRED. Implementation complete; human must review the draft PR. This agent must not merge or deploy.
4. **Current branch, commit, and phase** — `cursor/staging_gate3_500_fix` @ `a555b5a02ae00f069f1f95485213eb2306dab216`; draft PR open.
5. **Last completed task** — Staging reproduction, root-cause, migration, regression, local full pytest, evaluation, GitHub CI.
6. **Architecture summary** — Phase 8 canonical reads unchanged. Journal list remains AT-030 `JournalTradeService.list_trades` over PostgreSQL. The projector already wrote `account_id`; only the deployed schema was missing the column.
7. **Files changed** — `backend/src/app/db/migrations/versions/e8f1c4a9b702_journal_trades_account_id.py`; `backend/tests/test_journal_trades_alembic_empty_tenant.py`; Alembic head tests; `docs/deploy_rollback_runbook.md`; `docs/RELEASE_READINESS.md`.
8. **Important code references** — `GET /journal/trades` in `backend/src/app/api/routes/journal.py`; `JournalTrade.account_id` in `backend/src/app/db/models.py`; smoke step 10 in `scripts/canonical-staging-smoke.sh`.
9. **Tests and validation** —
    - Staging isolate: strategy-stats HTTP 200; journal list HTTP 500 (`UndefinedColumn` locally on Alembic head `d4f7a2c8e901`).
    - Regression before fix: FAILED (`column journal_trades.account_id does not exist`).
    - Regression after fix: PASSED.
    - Full backend pytest: 2273 passed, 237 warnings, exit 0 (1193s).
    - Ruff check: pass.
    - Strict mypy on new test module: no new errors (pre-existing `tests/support/postgres_persistence.py` arg-type via import).
    - Evaluation: agent 16/16, RAG 5/5, guardrails 7/7.
    - GitHub CI run 35519523042: success (backend, deployment-safety, frontend, docker-build, evaluation, e2e-smoke).
10. **Deployment and provider status** — Staging still on `main@4660172`. Paper-only; Watcher/Telegram disabled; `real_trading_enabled=false`. This change is not deployed. After a human merge, Render must apply `e8f1c4a9b702`.
11. **Security and trading safety status** — Unchanged. No Watcher/Telegram/live-trading enablement. Fail-closed tenant filtering preserved. Persistence errors are not converted to empty success.
12. **Decisions confirmed** — Do not weaken smoke. Empty tenant must 200. Missing schema is a real persistence defect and is fixed by migration, not by catching `ProgrammingError`.
13. **Known issues and blockers** — None for the code path. Staging remains broken until merge + migrate + smoke. Curl 56 on the original report was the smoke script's `-f` path around the 500; isolated curl without `-f` returned HTTP 500 JSON (curl 22 with `-f`).
14. **Remaining prioritized tasks** — Human review and merge of PR #106; staging redeploy; `INCLUDE_CANONICAL=true` smoke; do not enable Watcher/Telegram/live trading.
15. **Recommended model** — Grok 4.6 Extra High
16. **Exact next instruction for ChatGPT** — Treat PR #106 as the fix for Gate 3 canonical step 10. Do not merge or deploy from chat. After a human merge and staging migrate, re-run canonical smoke before calling staging recovered.
17. **Exact next Cursor prompt** — Do not merge or deploy. If asked to continue after merge: verify staging `/health` git_sha, confirm Alembic head `e8f1c4a9b702`, re-run `INCLUDE_CANONICAL=true COOKIE_MODE=true ./scripts/post-deploy-smoke-gate.sh`.

<!--
When Status is REVIEW_REQUIRED, BLOCKED, or FAILED, also include (§13.5):
- exact blocker: human merge/deploy authorization (user forbade merge/deploy)
- exact command or action that failed: original staging GET /journal/trades HTTP 500
- concise relevant error: column journal_trades.account_id does not exist
- what was already completed: diagnosis, migration, regression, local+CI validation, draft PR
- what remains unchanged: paper-only posture; Watcher/Telegram off; staging still on 4660172
- risk of continuing: merging without applying the migration would leave staging broken; this agent must not merge
- whether application code was modified: yes (Alembic + tests + docs)
- whether uncommitted work exists: only gitignored/local HANDOFF files
- exact human action required: review draft PR #106; merge only with explicit approval; then redeploy staging and re-run canonical smoke
- exact Cursor instruction after resolution: verify staging SHA + migration + INCLUDE_CANONICAL smoke
-->
