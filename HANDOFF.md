Status: FAILED
Last Updated: 2026-09-25T14:40:16+00:00
Task: Staging BTCUSDT market-status validation on deployed daa0532
Current Phase: Read-only staging validation
Progress: Health and safety posture confirmed. One usable Bybit observation, then the trade window failed closed. 3 consecutive usable observations were not reached.
Blocker: Canonical BTCUSDT market-status did not produce 3 consecutive usable live marks.
Human Action Needed: None
Next Step: Do not arm Watcher. Treat staging perpetual evidence as NOT READY.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-25T14:40:16+00:00
Generated At UTC: 2026-09-25T14:40:16Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260925-144016
Task ID: STAGING-BTCUSDT-MARKET-STATUS
Current Branch: main
Current Commit: daa0532
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: FAILED
Source File SHA256: eead37196f44453757034cec8a5592c6f89248e0be482d81efa761ac7ce13044
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Read-only validation of deployed `daa0532c3465685b46b518cf8041fa45a76d2367` on `https://alphatrade-api-staging.onrender.com` failed the 3-consecutive usable BTCUSDT bar. `GET /health` matches that SHA, paper mode, and disarmed Watcher/Telegram. Authenticated `GET /canonical/market-status?symbol=BTCUSDT` showed Binance banned and Bybit as the active public perpetual source. Bybit returned one fresh live mark (`83862.80`, age `0.788s`) and then repeated `IncompleteTradeWindowError` with no price. Result: **FAIL**. Final verdict: **NOT READY**.

2. **Goal** — Confirm three consecutive usable BTCUSDT observations on the canonical market-status path: real perpetual price, freshness under 10 seconds, `fallback_used=false`, source family `binance_usdm_futures_public` or `bybit_usdt_perpetual_public`, with usable OHLCV and trade/CVD evidence. If Binance is banned, Bybit must supply the observation. Do not modify code, deploy, change Render, or arm Watcher or Telegram.

3. **Current status** — `FAILED`. Staging is not ready to arm Watcher.

4. **Current branch, commit, and phase** — Local checkout `main` at `daa0532` (full `daa0532c3465685b46b518cf8041fa45a76d2367`). No product files changed. Phase: read-only staging validation, stopped after the usable streak was not reached.

5. **Last completed task** — Fast read-only staging market-status validation. Stopped without waiting through a long Binance ban window.

6. **Architecture summary** — Canonical path is authenticated `GET /canonical/market-status?symbol=BTCUSDT`. Staging health reports primary `binance_usdm`, secondary `bybit_usdt_perpetual`, activation `active`, freshness limit 10 seconds. A Binance ban is supposed to switch the whole observation to Bybit rather than copy a Binance price.

7. **Files changed** — No application code, config, or Render changes. This handoff and `CHANGELOG_SESSION.md` are the only documents written.

8. **Important code references** —
   - `GET /canonical/market-status` in `backend/src/app/api/routes/canonical.py`
   - Failover switch in `backend/src/app/market_contracts/adapters/failover.py`
   - Usable-price and trade-window projection in `backend/src/app/market_monitor/runtime.py`

9. **Tests and validation** —
   - `GET /health` after a cold-start timeout: HTTP 200 in 23.4s. `git_sha=daa0532c3465685b46b518cf8041fa45a76d2367`. `environment=staging`, `execution_mode=paper`, `real_trading_enabled=false`, `exchange_mode=paper_internal`.
   - Evidence: `perpetual_evidence_source=binance_usdm`, `perpetual_evidence_secondary_source=bybit_usdt_perpetual`, `perpetual_evidence_activation=active`, `live_quote_freshness_seconds=10`, `first_perpetual_symbol=BTCUSDT`, `live_market_read_only=true`, credentials/spot/fabricated fallback flags false.
   - Watcher flags false: `market_watcher_enabled`, `market_watcher_bridge_enabled`, `watcher_orchestration_enabled`, `watcher_paper_staging_activation`. Worker watcher `health_state=UNAVAILABLE`.
   - Telegram flags false: alerts, interaction, automatic delivery, `telegram_paper_activation_armed`, `telegram_network_permitted`, inbound mode `off`. `GET /health/telegram-paper-activation` verdict `NOT_ARMED`. Worker telegram `health_state=UNAVAILABLE`.
   - Isolated temporary readers were registered only to call market-status. No demo password was used. Tokens were not recorded in this handoff.
   - Eight market-status reads from 14:37:31Z through 14:37:51Z: Bybit identity with complete OHLCV (`latest_15m_close=83606.8`) but no price; trade coverage unknown, gap suspected, CVD unavailable, `IncompleteTradeWindowError`. The eighth read projected Binance with provider detail `Binance USD-M temporarily banned this client; no spot fallback` and still no price.
   - One usable read at `2026-09-25T14:38:31.640274Z`: source family `bybit_usdt_perpetual_public`, provider `bybit-usdt-perpetual`, instrument `bybit:usdm_futures:perpetual:BTCUSDT`, price `83862.80`, presentation `live_mark`, freshness state `fresh`, age `0.788274` seconds, `fallback_used=false`, OHLCV 15m/4h `complete`, trade coverage `complete`, CVD available with 135 events, `watcher_activated=false`.
   - The next reads, through `2026-09-25T14:39:55.531916Z`, returned to Bybit `IncompleteTradeWindowError`: availability `degraded`, price null, CVD unavailable, OHLCV still complete at `83606.8`. Backoff remaining stayed under 16 seconds. No long Binance Retry-After was waited out.
   - Consecutive usable count: 0 at stop (the single pass was followed by failures). **FAIL**.
   - Local unit tests were not run. This was a deployed read-only check.
   - Mac handoff sync script `~/.local/bin/sync-alphatrade-ai-handoff.sh` is absent on this host. iCloud copy was not performed.

10. **Deployment and provider status** — No deploy and no Render change. Active provider on the usable read and on the latest failing reads: `bybit-usdt-perpetual` / `bybit_usdt_perpetual_public`. Configured primary remains `binance_usdm`. Binance client ban was reported by the provider detail. Bybit supplied the venue, a complete OHLCV book, and one live mark, then failed the trade window closed. HTTP `source.venue` and `source.adapter_version` still read as Binance on a Bybit observation (`binance`, `binance-usdm-perpetual/v1`); instrument id, provider name, and source family identified Bybit.

11. **Security and trading safety status** — Paper only. Real trading false. Exchange mode `paper_internal`. Live market path read-only. No exchange credentials used for market evidence. Watcher off. Telegram off and `NOT_ARMED`. This run did not arm either one.

12. **Decisions confirmed** — A single fresh Bybit mark is not three consecutive usable observations. Short Bybit reconnect gaps were sampled. A long Binance ban wait was not entered. Watcher stays disarmed.

13. **Known issues and blockers** —
    - Exact blocker: `GET /canonical/market-status?symbol=BTCUSDT` did not yield 3 consecutive usable observations.
    - Exact failing condition: `IncompleteTradeWindowError`, availability `degraded`, reason `gap` or `reconnecting`, price null, CVD `incomplete_or_unhealthy_stream`.
    - One earlier Binance projection while banned did not carry a Bybit price.
    - Completed: SHA match, paper/safety posture, Watcher off, Telegram off, one Bybit live mark, OHLCV complete on Bybit.
    - Unchanged: application code, Render, Watcher, Telegram.
    - Risk of continuing: arming Watcher on a trade stream that drops to an incomplete window.
    - Application code was not modified. No uncommitted product work.
    - Human action required: none for this validation. Do not arm Watcher on this evidence.
    - Cursor instruction after resolution: rerun this same read-only 3-consecutive market-status check after the trade window stays complete; do not arm from this handoff.

14. **Remaining prioritized tasks** — None in this run. Do not arm Watcher or Telegram.

15. **Recommended model** — A current Cursor coding model is enough for a rerun. No product change is authorized by this result.

16. **Exact next instruction for ChatGPT** — Staging `daa0532` is NOT READY to arm Watcher. Binance reported a client ban. Bybit produced one fresh BTCUSDT mark at 83862.80 and then failed closed on `IncompleteTradeWindowError`. Require a new 3-consecutive pass before any arming discussion.

17. **Exact next Cursor prompt** —

```
TARGET: READ-ONLY STAGING RECHECK — BTCUSDT market-status
Do not modify code. Do not deploy. Do not change Render. Do not arm Watcher or Telegram.
Use an isolated temporary staging reader.
On https://alphatrade-api-staging.onrender.com confirm git_sha daa0532 and GET /canonical/market-status?symbol=BTCUSDT.
Stop at 3 consecutive usable observations (real perpetual price, freshness < 10s, fallback_used=false, source_family binance_usdm_futures_public or bybit_usdt_perpetual_public, usable OHLCV and trade/CVD).
If Binance is banned, require Bybit to supply those observations.
Do not wait through a long Binance ban. Maximum runtime 10 minutes.
Report 3/3 PASS or FAIL and READY TO ARM WATCHER or NOT READY.
```
