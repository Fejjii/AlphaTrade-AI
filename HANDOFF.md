Status: READY
Last Updated: 2026-09-24T12:16:45+00:00
Task: Diagnose intermittent BTCUSDT RegionalProviderFailureError
Current Phase: Read-only diagnosis complete
Progress: Staging reports HTTP 418 from fapi.binance.com. The monitor then latches closed.
Blocker: None
Human Action Needed: None
Next Step: Remediate 418 handling and the unrecoverable-gap latch before any Watcher arming. Do not deploy from this note.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-24T12:16:45+00:00
Generated At UTC: 2026-09-24T12:16:45Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260924-121645
Task ID: LIVE-MARKET-418-DIAGNOSIS
Current Branch: cursor/handoff-live-market-validation-c194
Current Commit: 697cc13
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: READY
Source File SHA256: e871a336c2ab2488dd5b983dfd8794ac7cafd55820a5eede6dfdfe4f096a1d3c
END METADATA -->

# AlphaTrade AI — Handoff

## Diagnosis result

ROOT CAUSE: Binance USD-M `GET /fapi/v1/ping` from the staging API returns HTTP 418. The adapter maps 418 to `RegionalProviderFailureError` and does not retry it. After repeated trade-fetch failures the in-memory monitor calls reconnect and then latches `UnrecoverableGapError`, so later market-status reads no longer fetch trades.

SUCCESS RATE: 14/24 provider pings healthy (58%). Authenticated market-status fresh marks in this sample: 0/20 (0%).

FAILURE RATE: 10/24 provider pings HTTP 418 (42%). Market-status: 20/20 unavailable (100%), of which 3 were `RegionalProviderFailureError` and 17 were the latched `UnrecoverableGapError`.

HTTP/error evidence: `GET /providers/status` `error_message` was only `Preferred Binance USD-M source rejected the runtime region (HTTP 418).` Failures were about 2.6–5.5s, not the 10s timeout. No `RateLimitedError` (429). Render log API was not available here. Application revision `697cc13`. No code change, no deploy, no activation.

Code change required: yes. 418 must be treated as a ban/backoff, and a failed reconnect must not permanently close the stream.

Lowest-risk remediation: keep Watcher and Telegram disarmed. Change the adapter so HTTP 418 uses a bounded backoff instead of the regional no-retry path, and change the monitor so an empty or watermark-missing backfill does not stick at `UNRECOVERABLE` until process restart. Do not raise the request budget.

FINAL VERDICT:
READY FOR REMEDIATION

1. **Executive summary** — Read-only diagnosis of intermittent staging BTCUSDT failures. The upstream status is HTTP 418 from `fapi.binance.com`, classified as `RegionalProviderFailureError`. The monitor can then close the stream for the life of the process. LIVE MARKET VALIDATION remains FAIL. Arming verdict remains NOT READY. This diagnosis verdict is READY FOR REMEDIATION.

2. **Goal** — Name the cause of the intermittent provider failure and quantify a bounded sample. No implementation.

3. **Current status** — READY as the diagnosis record. Do not arm Watcher.

4. **Current branch, commit, and phase** — Application revision `697cc13482a436bf0911a5949b3ac89e6cfbd68f`. Handoff-only branch. No product diff.

5. **Last completed task** — Staging provider-status and authenticated market-status sampling.

6. **Architecture summary** — `ReadOnlyHttpGetClient` treats HTTP 401, 403, 418, and 451 as regional and does not retry them. 429 is `RateLimitedError` and is retried. Connect and timeout errors retry up to `binance_request_max_retries` (code default 3; not overridden in `render.yaml`). `SymbolMonitorRuntime` starts reconnect on `RegionalProviderFailureError`. `recover_from_backfill` marks the cursor `UNRECOVERABLE` when the backfill is empty or does not cover the watermark, and later ticks re-raise that error without a new trade fetch. Lookback is capped at 10 minutes.

7. **Files changed** — `HANDOFF.md` and `CHANGELOG_SESSION.md` only.

8. **Important code references** —
   - `backend/src/app/market_contracts/adapters/http.py` (`REGIONAL_STATUS_CODES` includes 418)
   - `backend/src/app/market_contracts/adapters/binance_usdm.py` (`status` records the exception text)
   - `backend/src/app/market_monitor/runtime.py`
   - `backend/src/app/market_contracts/cursor.py` (`recover_from_backfill`)
   - `render.yaml` region `frankfurt`, `MARKET_DATA_FUTURES_BASE_URL=https://fapi.binance.com`

9. **Tests and validation** —
   - `GET /providers/status` × 24. 14 healthy, 10 `HTTP 418`. Every failure string was the 418 regional message.
   - Authenticated `GET /canonical/market-status?symbol=BTCUSDT` × 20. 0 fresh prices. Attempts 1–3 were `RegionalProviderFailureError` (0.678s, 2.594s, 2.270s) with the following ping still healthy. The fourth read took 16.112s and became `UnrecoverableGapError`. The remaining 16 reads stayed on that error at about 0.4s.
   - Ruled out for these failures: 429 rate-limit branch, request-budget `RateLimitedError`, cache hit, spot fallback, fabricated price, and a 10-second timeout. This runner’s own direct `fapi.binance.com` calls return HTTP 451 and are a different network path.
   - Render log credentials were not present. The HTTP code came from the staging provider status payload.

10. **Deployment and provider status** — No deploy. No Render edits. Staging API remained up. Frankfurt egress can still complete some public USD-M pings. 418 is intermittent on that path.

11. **Security and trading safety status** — `execution_mode=paper`, `real_trading_enabled=false`. Watcher and Telegram were not armed. One additional auth registration was used for the market-status sample. No orders.

12. **Decisions confirmed** — Do not arm Watcher on this process. A code change is required before the failure mode is safe to ignore.

13. **Known issues and blockers** — The current API process can stay on `UnrecoverableGapError` until restart. Restart was not performed.

14. **Remaining prioritized tasks** — Remediate 418 backoff and the permanent gap latch. Then repeat consecutive authenticated BTCUSDT reads.

15. **Recommended model** — A careful coding model for the small adapter and monitor change.

16. **Exact next instruction for ChatGPT** — Staging `fapi.binance.com` failures are HTTP 418, stored as `RegionalProviderFailureError`. The monitor can latch `UnrecoverableGapError`. Verdict READY FOR REMEDIATION. Do not arm Watcher or Telegram.

17. **Exact next Cursor prompt** — Fix HTTP 418 handling in the read-only Binance USD-M client so a ban backs off instead of being treated as a permanent regional failure, and stop `recover_from_backfill` from latching the monitor closed when the backfill cannot cover the watermark. Add tests. Do not deploy, do not change Render, and do not arm Watcher or Telegram.
