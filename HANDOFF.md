Status: READY
Last Updated: 2026-09-23T14:10:06+00:00
Task: AT-084 — Final Render Blueprint cleanup
Current Phase: Blueprint ready for a later human provision
Progress: alphatrade-worker-staging removed from render.yaml
Blocker: None
Human Action Needed: None
Next Step: Review draft PR 135. Do not provision or activate.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-23T14:10:06+00:00
Generated At UTC: 2026-09-23T14:10:06Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260923-141006
Task ID: AT-084
Current Branch: cursor/render_blueprint_final_cleanup
Current Commit: 2475af8
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: READY
Source File SHA256: 85014790ecf25657c62ef2aa92f1cd56113964fef32381de552ea8e3b6805a9a
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — `alphatrade-worker-staging` is not required. The Blueprint is the existing API plus disarmed Watcher and Telegram workers. Draft PR https://github.com/Fejjii/AlphaTrade-AI/pull/135. Not provisioned.
2. **Goal** — Final Render Blueprint cleanup before a later paper-runtime provision.
3. **Current status** — READY. Verdict: BLUEPRINT READY FOR PROVISIONING.
4. **Current branch, commit, and phase** — `cursor/render_blueprint_final_cleanup` at `2475af8`, based on `main@6314a2021441384b9a1bb20f6c0b82916b15f6bd`.
5. **Last completed task** — AT-084. ADR AT-ADR-063.
6. **Architecture summary** — Vercel frontend. Render: `alphatrade-api-staging`, `alphatrade-watcher-paper-staging` (`python -m app.workers.watcher_paper`), `alphatrade-telegram-paper-staging` (`python -m app.telegram_activation run`). Slice 59 `app.workers.entrypoint` remains in the repo and is not a Blueprint service.
7. **Files changed** — `render.yaml`, `backend/tests/test_disarmed_render_worker_boot.py`, `backend/tests/test_live_market_activation.py`, `docs/RELEASE_READINESS.md`, `docs/controlled_paper_activation.md`, `.ai/DECISIONS.md`, `.ai/TASKS.md`.
8. **Important code references** — `app.workers.entrypoint` (legacy scan, one backtest drain, notifier). `app.workers.watcher_paper` (paper scans, Candidates, evaluation records). `app.telegram_activation` runtime (outbox and polling). `enqueue_backtest_if_needed` drains backtests when `WORKER_ENABLED` is false. `CanonicalPaperExecutionService` is the paper execution and journal path.
9. **Tests and validation** — `uv run ruff check` and `uv run ruff format --check` on the two edited tests, exit 0. `uv run pytest` exit 0: 122 passed, 0 failed, 0 skipped (`test_disarmed_render_worker_boot.py` 17, `test_deployment_safety.py` 51, `test_deployment_scripts.py` 14, `test_config.py` 8, `test_watcher_paper_activation.py` 29, plus one test each from live-market config, frontier blueprint, and final-three blueprint boot). Full backend suite not rerun. Application runtime code unchanged. GitHub CI for this commit: UNKNOWN (not finished in this session).
10. **Deployment and provider status** — Not deployed. Not provisioned. Not activated. Render association: a service `name` in the Blueprint is applied to an existing service of that name (`https://render.com/docs/blueprint-spec`, fetched 2026-09-23). A second Blueprint from the same file creates an independent set and suffixes names. New workers default to auto-deploy on commit; omitted `autoDeployTrigger` is retained for the existing API. Omitted `branch` uses the repo default branch. `region: frankfurt` cannot be changed after creation. Starter pricing from `https://render.com/pricing` on 2026-09-23: web $7/month, worker $7/month, no Free worker. Expected new compute is two Starter workers. The removed service avoids a third Starter worker. Workspace fees, bandwidth, and build minutes are UNKNOWN.
11. **Security and trading safety status** — `EXECUTION_MODE=paper`, `ENABLE_REAL_TRADING=false`, `EXCHANGE_MODE=paper_internal`. Watcher and Telegram flags stay false. No bot token and no exchange credentials in `render.yaml`. Secrets remain `sync: false`.
12. **Decisions confirmed** — AT-ADR-063. The Slice 59 worker is superseded for this Blueprint by the Watcher and Telegram processes plus the API execution path.
13. **Known issues and blockers** — None. Live Render service region, branch, and plan for `alphatrade-api-staging` were not read from the dashboard. Confirm Frankfurt, `main`, and Starter before a human sync, because a Blueprint sync applies this file to that existing service and can deploy it.
14. **Remaining prioritized tasks** — Human review of PR 135. Do not provision from this run.
15. **Recommended model** — Grok 4.6 Extra High
16. **Exact next instruction for ChatGPT** — Treat the Blueprint as ready for a later human provision. Do not deploy, provision, or arm Watcher or Telegram.
17. **Exact next Cursor prompt** — Review PR 135 only. Do not sync `render.yaml` to Render and do not change Watcher or Telegram flags.
