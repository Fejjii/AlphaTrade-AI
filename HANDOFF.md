Status: READY
Last Updated: 2026-09-21T17:48:00+00:00
Task: AT-072 — Integrate paper Watcher stack (PR118 → PR120 → PR119)
Current Phase: Independent review
Progress: Integration complete; exact-HEAD GitHub CI green; Watcher stays off
Blocker: None
Human Action Needed: Independent review of draft PR #121. Do not merge, deploy, or activate Watcher.
Next Step: Review PR #121. Do not merge, deploy, or activate Watcher.

<!-- METADATA
Project: AlphaTrade AI
Document Type: HANDOFF
Schema Version: 2.0
Generated At Local: 2026-09-21T17:48:00+00:00
Generated At UTC: 2026-09-21T17:48:00Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260921-174800
Task ID: AT-072
Current Branch: cursor/watcher_integration-b74b
Current Commit: 69df737e211133786122ace76fec31eed1b237a0
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: READY
Source File SHA256: 99f1fad08ecdf28a755bc09a766dcdada2725e887bd5045fcacc4700531ecea0
END METADATA -->

# AlphaTrade AI — Handoff

1. **Executive summary** — Paper Watcher stack is integrated on
   `cursor/watcher_integration-b74b` from `main@b4244f0` by cherry-picking PR
   #118, #120, then #119 without merging those source PRs. One evidence
   authority: live/read-only monitor gates current quote and trade stream;
   `FirstSliceEvidenceAssembler` is the sole `CanonicalEvidenceWindowV1`
   producer. One evaluator path mints Candidates only on `CONFIRMED_SETUP`
   from persisted approved compiled authority. Monitoring `RUNNING` requires
   a fenced lease plus a fresh heartbeat. Watcher, Telegram, and live trading
   stay off. No deploy. Exact-HEAD GitHub CI run
   [35631036137](https://github.com/Fejjii/AlphaTrade-AI/actions/runs/35631036137)
   succeeded on `69df737`. **FINAL VERDICT: READY FOR INDEPENDENT REVIEW.**

2. **Goal** — Integrate and harden the complete AlphaTrade paper Watcher
   stack: one evidence authority, one evaluator, truthful monitoring,
   fail-closed freshness, worker safety, UI honesty, and integration tests
   proving mint plus fail-closed refusals. Draft integration PR only.

3. **Current status** — READY. Product HEAD
   `69df737e211133786122ace76fec31eed1b237a0`. Draft PR
   https://github.com/Fejjii/AlphaTrade-AI/pull/121. Source PRs #118/#120/#119
   were not merged. Watcher was not activated. Nothing was deployed.

4. **Current branch, commit, and phase** — Product branch
   `cursor/watcher_integration-b74b` at `69df737`. Handoff publish branch
   `cursor/watcher-integration-handoff-b74b` (no PR). Phase: independent
   review.

5. **Last completed task** — AT-072 Watcher stack integration and exact-HEAD
   GitHub CI on `69df737`.

6. **Architecture summary** —

   ```text
   PerpetualMarketSource (replay default | binance_usdm)
           │
           ├─ PerpetualMarketMonitor.latest()
           │         │
           │         └─ watcher_evidence_error_for_monitor
           │            allow: REPLAY | FRESH | DEGRADED+RATE_LIMITED with FRESH/AGING quote
           │            refuse: stale_evidence | provider_outage | canonical_evidence_unavailable
           │
           └─ FirstSliceEvidenceAssembler  (sole CanonicalEvidenceWindowV1)
                     │
                     └─ AssemblingWatcherScanEvidence
                               │ mode mismatch → wrong_source
                               ▼
               persisted APPROVED/ACTIVE strategy
                               ▼
               executable compiled definition
                               ▼
               evaluate_canonical_strategy → SetupAssessment
                               ▼
               CONFIRMED_SETUP only → Candidate
                               │
   WatcherPaperRuntime (single lease, heartbeat, stale-worker fence)
                               ▼
   WatcherMonitoringService → GET /market-watcher/monitoring
                               ▼
   WatcherMonitoringCard (RUNNING/STOPPED/DEGRADED/STALE/BLOCKED)
   ```

   Shared source: replay monitor with replay assembler, live monitor with
   live assembler. Replay is allowed for deterministic tests and is never a
   live perpetual mark. No spot fallback. No fabricated prices. No second
   evaluator. In-memory policy authority cannot mint.

7. **Files changed** — Cherry-picks of #118/#120/#119 plus integration
   overlay (`e01d7ea..69df737`, 23 files, +1232/−47): `watcher_gate.py`,
   evidence watcher port, paper factory/lifespan, monitoring service/schemas/
   state/UI clocks, in-memory `list_for_organization`,
   `test_watcher_stack_integration.py`, AT-ADR-051 / AT-072 docs. Full PR vs
   `main`: 94 files, +9323/−38. `HANDOFF.md` / `CHANGELOG_SESSION.md` are not
   part of the product PR.

8. **Important code references** — `app.market_monitor.watcher_gate`;
   `AssemblingWatcherScanEvidence`; `default_paper_evidence_factory`;
   `paper_runtime_enabled`; `WatcherMonitoringService` /
   `project_watcher_monitoring_state`; `WATCHER_PAPER_ACTIVATION_REQUIREMENTS`;
   `backend/tests/test_watcher_stack_integration.py`.

9. **Tests and validation** —

   | Check | Result |
   | --- | --- |
   | Focused Watcher/monitor/runtime/monitoring pytest | 115 passed, exit 0 |
   | Full backend with PostgreSQL (`AT028_POSTGRES_URL`) | 2464 passed / 0 skipped, exit 0 |
   | Frontend lint + typecheck + unit tests + build | 1193 tests passed; lint/typecheck/build exit 0 |
   | Ruff check/format | exit 0 (local + GitHub CI) |
   | Strict mypy on affected modules | exit 0 |
   | Alembic | single head `c8d9e0f1a2b3` |
   | Evaluation | agent 16/16, RAG 5/5, guardrails 7/7 |
   | Chromium E2E | 30 passed / 13 skipped |
   | Deployment safety | 60 passed; smoke self-checks from repo root exit 0 |
   | Docker | image `alphatrade-backend:ci` built locally; GitHub `docker-build` success |
   | GitHub CI `cc226f7` | run 35627618727 success (prior code HEAD) |
   | GitHub CI exact HEAD `69df737` | run 35631036137 **success** (backend, frontend, evaluation, e2e-smoke, deployment-safety, docker-build) |

   **Skipped (reported separately):**
   - Chromium E2E: 13 skipped. Staging-backed Playwright specs skip without
     `STAGING_BASE_URL` / `STAGING_DEMO_PASSWORD` (portfolio, strategy-quality,
     session-observations, run-sessions, setup-alert-review, analytics-hub,
     validation-priority, alerts-readonly, setup-alert-draft, lessons,
     learning-analytics, coaching, plus one smoke skip). Chromium project
     ignores `capture-screenshots.spec.ts` and `webkit-iphone-audit.spec.ts`.
   - Full backend with PostgreSQL: 0 skipped.
   - Local Redis was not used; tests use the memory limiter (`RATE_LIMIT_USE_REDIS=false` in E2E).
   - Live Binance USD-M current-quote activation was not exercised (replay remains default).

10. **Deployment and provider status** — No deploy. Replay remains the
    default market source. Staging/production still reject Watcher flags
    (`market_watcher_enabled`, `watcher_orchestration_enabled`, Telegram).
    `EXECUTION_MODE=paper`. `ENABLE_REAL_TRADING=false`. `real_trading_enabled`
    stays false.

11. **Security and trading safety status** — Watcher OFF by default.
    `paper_runtime_enabled` requires local + paper + orchestration flag +
    real trading false. Telegram off. No exchange mutation. Kill-switch
    BLOCK remains final. Draft PR only.

12. **Decisions confirmed** — AT-ADR-051. Do not merge source PRs. Do not
    deploy. Do not activate Watcher. Preserve replay. No spot fallback. No
    fabricated prices. One evaluator. Config never RUNNING. ID remaps:
    AT-069/048 monitor, AT-070/049 runtime, AT-071/050 UX, AT-072/051 this
    integration.

13. **Known issues and blockers** — None blocking independent review.
    Remaining activation is out of scope by design.

14. **Remaining prioritized tasks** —
    - Independent review of PR #121 (this verdict).
    - Separate authorized task to enable Watcher on **local paper only**.
    - Further safety/risk program before staging/production flags can change.
    - Do not merge source PRs #118/#120/#119 as part of this slice.
    - Source PRs remain unmerged on purpose.

    Remaining activation requirements (from
    `WATCHER_PAPER_ACTIVATION_REQUIREMENTS`):
    1. `WATCHER_ORCHESTRATION_ENABLED` remains false in staging and production
    2. Local paper enablement only (`ENVIRONMENT=local`, `EXECUTION_MODE=paper`)
    3. `ENABLE_REAL_TRADING` stays false; no real exchange credentials
    4. At least one tenant-scoped APPROVED or ACTIVE compiled strategy
    5. Read-only perpetual evidence (replay default, or `binance_usdm`)
    6. Dedicated paper Watcher process or local autostart
    7. Telegram stays disabled; Watcher never places orders

    Remaining gaps (honest, not blockers for this slice):
    - Watcher process is not running in staging/production and is off by default locally.
    - CONFIRMED_SETUP mint tests use gated world fixtures because live/replay
      assembler windows do not independently confirm; production factory still
      uses the assembler behind the monitor gate.
    - `.ai/TASKS.md` completion evidence still cites CI 35627618727 on
      `cc226f7`; exact HEAD is docs-only `69df737` with CI 35631036137.
    - Independent review has not happened yet.

15. **Recommended model** — Cursor Grok 4.6 Extra High

16. **Exact next instruction for ChatGPT** — Independent review of
    https://github.com/Fejjii/AlphaTrade-AI/pull/121 at
    `69df737e211133786122ace76fec31eed1b237a0`. Do not merge, deploy, or
    activate Watcher. If a follow-up is needed, it is a **separate** local
    paper Watcher activation task with explicit authorization. Source PRs
    #118/#119/#120 stay unmerged.

17. **Exact next Cursor prompt** — Independent review of draft PR #121
    (`cursor/watcher_integration-b74b` @ `69df737`). Do not merge. Do not
    deploy. Do not activate Watcher. Do not merge source PRs #118, #119, or
    #120. If review finds defects, fix on this branch and re-run exact-HEAD
    GitHub CI. If review is clean, stop; activation is a later authorized
    task.
