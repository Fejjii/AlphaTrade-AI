<!-- METADATA
Project: AlphaTrade AI
Document Type: CHANGELOG_SESSION
Schema Version: 2.0
Generated At Local: 2026-09-25T14:40:16+00:00
Generated At UTC: 2026-09-25T14:40:16Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260925-144016
Task ID: STAGING-BTCUSDT-MARKET-STATUS
Current Branch: main
Current Commit: daa0532
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: FAILED
Source File SHA256: 025803724226789005b2a3dfaa181961d7f0eb819d721758bf92b3b8d3b605aa
END METADATA -->

# AlphaTrade AI — Session Changelog (latest session only)

## 1. Session metadata —
Session `AT-SESSION-20260925-144016`. Read-only staging validation of deployed `daa0532`. Host timezone `Etc/UTC`. No product code changes.

## 2. Starting state —
Checkout `main` at `daa0532c3465685b46b518cf8041fa45a76d2367`, clean tree. Staging API `https://alphatrade-api-staging.onrender.com`. Watcher and Telegram were not armed by this session.

## 3. Work performed —
Confirmed `GET /health` and `GET /health/telegram-paper-activation`. Registered isolated temporary staging readers and called `GET /canonical/market-status?symbol=BTCUSDT`. Stopped when three consecutive usable observations were not obtained and the trade window kept failing closed on short Bybit backoff. Did not wait out a long Binance ban.

## 4. Files created, changed, or removed —
No application files. Wrote `HANDOFF.md` and `CHANGELOG_SESSION.md` only.

## 5. Commands and tests run —
- `GET /health` (first attempt timed out at 70s during cold start; retry HTTP 200 in 23.4s).
- `GET /health/telegram-paper-activation` HTTP 200, verdict `NOT_ARMED`.
- `POST /auth/register` for isolated readers (one HTTP 500 when the organization name was reused; later unique organization names returned HTTP 201).
- Repeated `GET /canonical/market-status?symbol=BTCUSDT` from 14:37:31Z through 14:39:55Z.
- `POST /auth/logout` for readers whose access tokens were still held.
- Local pytest was not run.

## 6. Exact results —
Health SHA `daa0532c3465685b46b518cf8041fa45a76d2367`. Paper, real trading false, exchange `paper_internal`. Primary `binance_usdm`, secondary `bybit_usdt_perpetual`, activation active. Watcher flags false. Telegram flags false. Worker watcher and telegram `UNAVAILABLE`.

Market-status: Bybit was the active family on the usable read and on the final failing reads. One pass at `2026-09-25T14:38:31.640274Z`: price `83862.80`, age `0.788274` seconds, `fallback_used=false`, OHLCV 15m/4h complete, CVD 135 events, coverage complete. Following reads: `IncompleteTradeWindowError`, price null, CVD unavailable, OHLCV still complete at `83606.8`. An intermediate Binance projection reported `Binance USD-M temporarily banned this client; no spot fallback` and had no price. Consecutive usable observations: not 3. **FAIL**. Verdict **NOT READY**.

## 7. Latest successful step —
Safety posture and deployed SHA were confirmed, and one Bybit live mark was observed. The consecutive-usability bar was not met.

## 8. Blockers or review requests —
Blocker: trade window did not stay complete long enough for 3 consecutive usable BTCUSDT observations. No human approval is requested. Do not arm Watcher.

## 9. Warnings and risks —
Arming Watcher on this stream would consume a source that drops to `gap` / `reconnecting` with no fresh price. HTTP venue and adapter version fields still say Binance when the instrument, provider name, and source family are Bybit.

## 10. Follow-up actions —
Rerun the same read-only check later. Do not deploy, do not change Render, and do not arm Watcher or Telegram from this result. Mac iCloud sync script is absent on this host; publish this handoff on a dedicated GitHub handoff branch only.

## 11. Final status —
`FAILED`. Staging BTCUSDT market-status is **NOT READY** to arm Watcher.
