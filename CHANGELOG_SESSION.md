<!-- METADATA
Project: AlphaTrade AI
Document Type: CHANGELOG_SESSION
Schema Version: 2.0
Generated At Local: 2026-09-24T13:10:44+00:00
Generated At UTC: 2026-09-24T13:10:44Z
Timezone: UTC
Session ID: AT-SESSION-20260924-123856
Task ID: AT-085
Current Branch: cursor/binance-418-recovery-11ce
Current Commit: f883f3b
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: IN_PROGRESS
Source File SHA256: 947d74780aff98cc13accdd6d2c9aa9ee9e84f5b7bfb55e55a8f787ff67f72e4
END METADATA -->

# AlphaTrade AI — Session Changelog (latest session only)

## 1. Session metadata —
AT-SESSION-20260924-123856. Task AT-085. Host timezone UTC. Status IN_PROGRESS.

## 2. Starting state —
Product commit `21c020f` was pushed and draft PR #138 opened.

## 3. Work performed —
Exact-head CI run 36000366161 failed the backend job. Three tests expected Watcher `provider_outage` after a regional failure. A later empty backfill had changed the monitor reason, so the gate returned `canonical_evidence_unavailable`. `f883f3b` keeps `PROVIDER_UNAVAILABLE` for a reconnect opened by `RegionalProviderFailureError` until recovery succeeds. Price stays unusable. Cursor and connection identity are unchanged.

## 4. Files created, changed, or removed —
`backend/src/app/market_monitor/runtime.py`. `backend/tests/test_binance_usdm_staging_reliability.py`.

## 5. Commands and tests run —
`gh run view 36000366161 --log-failed`. `uv run ruff check` and `uv run ruff format` on the edited files. `uv run pytest` on the two Watcher outage tests, the Postgres fail-closed loop test, and `tests/test_binance_usdm_staging_reliability.py`.

## 6. Exact results —
CI run 36000366161: backend failure, 3 failed, 2689 passed, 235 warnings. deployment-safety, frontend, and docker-build success. e2e-smoke and evaluation skipped. Local pytest after the fix exit 0, with 1 skip (Postgres not available). Ruff exit 0.

## 7. Latest successful step —
Pushed `f883f3b` to `cursor/binance-418-recovery-11ce`.

## 8. Blockers or review requests —
None. Do not merge, deploy, or activate.

## 9. Warnings and risks —
The Postgres fail-closed test was not executed locally. CI is the check for that case.

## 10. Follow-up actions —
Wait for exact-head CI on `f883f3b`. Then READY FOR STAGING REVALIDATION or NOT READY.

## 11. Final status —
IN_PROGRESS
