<!-- METADATA
Project: AlphaTrade AI
Document Type: CHANGELOG_SESSION
Schema Version: 2.0
Generated At Local: 2026-09-22T15:17:40+00:00
Generated At UTC: 2026-09-22T15:17:40Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260922-151740
Task ID: AT-077
Current Branch: cursor/activation_telegram_paper-a361
Current Commit: 4f244d6
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: READY
Source File SHA256: af166f8e859b9db780b5ca36e773b1844091f7e9510493f8d3834663a7485551
END METADATA -->

# AlphaTrade AI — Session Changelog (latest session only)

## 1. Session metadata —
AT-077 controlled Telegram paper activation. Host timezone Etc/UTC. Product branch `cursor/activation_telegram_paper-a361` at `4f244d6`. Draft PR https://github.com/Fejjii/AlphaTrade-AI/pull/129.

## 2. Starting state —
`main` at `894e9e4`. Telegram paper discussion and the security outbox existed and stayed disabled. Webhook, activation arming, durable activation cursor/ledger, and rollback procedure were absent. Watcher `main()` does not install a Telegram hook.

## 3. Work performed —
Added `app.telegram_activation` for preflight, arm/disarm, outbox delivery, polling and webhook intake, recipient binding checks, send ledger, inbound cursor, health posture, and a rollback checklist. Extended the security protocol so retry waits are computed from durable `attempt` and `updated_at`, and `RATE_LIMITED` does not increment the attempt. Alembic `e0f1a2b3c4d5` adds `telegram_activation_inbound_cursors` and `telegram_activation_send_ledger`. Staging and production deployment safety reject the new arming flags. `render.yaml` was not changed. `create_app` does not mount the webhook. No live Telegram message was sent. Nothing was deployed or armed.

## 4. Files created, changed, or removed —
See commit `4f244d6`. New package `backend/src/app/telegram_activation/`, migration `e0f1a2b3c4d5`, scripts under `scripts/telegram-paper-activation-*.sh`, and `docs/telegram_paper_activation.md`.

## 5. Commands and tests run —
- Local `uv run ruff check .` and `uv run ruff format --check .`
- Local `uv run mypy --strict` on the activation modules and related stores
- Local `uv run pytest -q --tb=line` against PostgreSQL 16, then a re-run of the two route allowlist tests plus activation, activation-postgres, and Alembic head tests
- Frontend `npm run lint`, `typecheck`, `test`, `build`
- Local evaluation agent, RAG, and guardrails
- `python -m app.telegram_activation preflight --expect-disabled` and `rollback` / `rollback --apply`
- GitHub CI run 35742331530 on `4f244d65eb4e36d8f5d65c2e2b03f4c7b02e1243`

## 6. Exact results —
Local Ruff passed. mypy --strict passed on the activation modules. Full local pytest failed 2 tests until `GET /health/telegram-paper-activation` was added to the known non-inbound route set; the follow-up run passed 20 tests. Frontend: lint clean, typecheck clean, 1195 tests passed, build succeeded. Evaluation 16/16, RAG 5/5, guardrails 7/7. Preflight exit 0 with verdict `NOT_ARMED`. Rollback exit 0. Rollback `--apply` exit 2. CI conclusion success: backend, deployment-safety, docker-build, frontend, evaluation, e2e-smoke. https://github.com/Fejjii/AlphaTrade-AI/actions/runs/35742331530

## 7. Latest successful step —
CI run 35742331530 succeeded. Verdict is READY FOR CONTROLLED ACTIVATION. The process remains disarmed.

## 8. Blockers or review requests —
None. No human approval is required to keep the process disarmed. Draft PR #129 is ready for review. Do not merge it as an activation.

## 9. Warnings and risks —
Do not set `TELEGRAM_PAPER_ACTIVATION_ARMED`, `TELEGRAM_NETWORK_PERMITTED`, or a non-off inbound mode in staging or production. Do not install `paper_scan_hook()` on the Watcher worker as part of this task. The in-memory outbound window resets on process restart; the durable `RATE_LIMITED` wait remains on the outbox row.

## 10. Next recommended step —
Review draft PR #129. Leave activation off.
