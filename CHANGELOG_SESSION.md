<!-- METADATA
Project: AlphaTrade AI
Document Type: CHANGELOG_SESSION
Schema Version: 2.0
Generated At Local: 2026-09-24T11:59:17+00:00
Generated At UTC: 2026-09-24T11:59:17Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260924-115917
Task ID: LIVE-MARKET-STAGING-VALIDATION
Current Branch: main
Current Commit: 697cc13
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: READY
Source File SHA256: a31627771af00d0bdf8f881edbf8252cff0dcbc74c3b261a3558a9756e613ab2
END METADATA -->

# AlphaTrade AI — Session Changelog (latest session only)

## 1. Session metadata —
Read-only staging live market validation. Host timezone Etc/UTC. Application revision `697cc13`. Staging `git_sha` matched that revision.

## 2. Starting state —
Task was a remote read of `https://alphatrade-api-staging.onrender.com`. No code changes were authorized.

## 3. Work performed —
Ran `./scripts/validate-live-market-staging.sh --remote --require-active` twice (exit 0). Called unauthenticated market-status (401), then authenticated `GET /canonical/market-status?symbol=BTCUSDT` across several samples. Read `GET /health` and `GET /health/telegram-paper-activation`. Did not deploy, edit Render, arm Watcher, or arm Telegram.

## 4. Files created, changed, or removed —
Regenerated `HANDOFF.md` and `CHANGELOG_SESSION.md` only.

## 5. Commands and tests run —
- `BASE_URL=https://alphatrade-api-staging.onrender.com ./scripts/validate-live-market-staging.sh --remote --require-active` (twice, exit 0)
- `GET /health`
- `GET /canonical/market-status?symbol=BTCUSDT` unauthenticated and authenticated
- `GET /health/telegram-paper-activation`
- Direct `https://fapi.binance.com` from this runner (HTTP 451)

## 6. Exact results —
Health: `execution_mode=paper`, `real_trading_enabled=false`, `perpetual_evidence_source=binance_usdm`, `perpetual_evidence_activation=active`, freshness 10 seconds, first symbol BTCUSDT, credentials false, spot fallback false, fabricated fallback false, live market read-only true. Watcher and Telegram flags false. Telegram preflight `NOT_ARMED`.

Last fresh authenticated mark: price `83388.50`, source time `2026-09-24T11:57:54.950000Z`, freshness `fresh`, age `0.298184` seconds, family `binance_usdm_futures_public`, `fallback_used=false`, OHLCV available, CVD available (168 events), trades `3463934233`–`3463934400`. Earlier fresh mark `83409.90` at `2026-09-24T11:55:19.369000Z`. Interleaved reads returned no price with `RegionalProviderFailureError`.

LIVE MARKET VALIDATION: FAIL. FINAL VERDICT: NOT READY.

## 7. Latest successful step —
Safety posture and one or more fresh public USD-M marks were observed. The feed did not stay fresh on the following read.

## 8. Blockers or review requests —
None for this audit. Arming is not supported by the intermittent evidence.

## 9. Warnings and risks —
About half of the final authenticated samples had no usable price. Fail-closed behavior held (`fallback_used=false`, no spot substitute). Four auth registrations were created to obtain bearer tokens. This runner cannot call Binance directly (HTTP 451).

## 10. Follow-up actions —
Leave Watcher and Telegram disarmed. Re-validate consecutive fresh market-status reads before any arming task.

## 11. Final status —
READY as the audit record. Product arming verdict: NOT READY.
