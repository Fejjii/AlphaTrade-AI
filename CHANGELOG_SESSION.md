<!-- METADATA
Project: AlphaTrade AI
Document Type: CHANGELOG_SESSION
Schema Version: 2.0
Generated At Local: 2026-09-22T14:44:28+00:00
Generated At UTC: 2026-09-22T14:44:28Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260922-144428
Task ID: AT-077
Current Branch: cursor/activation_telegram_paper-a361
Current Commit: 4f244d6
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: IN_PROGRESS
Source File SHA256: f364795cc43b574443759cc9921ef4ba46267827ad8c6007f8183eeee384ced6
END METADATA -->

# AlphaTrade AI — Session Changelog (latest session only)

## 1. Session metadata —
AT-077 controlled Telegram paper activation. Host timezone Etc/UTC. Branch `cursor/activation_telegram_paper-a361` at `4f244d6`. Draft PR https://github.com/Fejjii/AlphaTrade-AI/pull/129.

## 2. Starting state —
`main` at `894e9e4`. Telegram paper discussion and the security outbox existed and stayed disabled. Webhook, activation arming, durable activation cursor/ledger, and rollback procedure were absent. Watcher `main()` does not install a Telegram hook.

## 3. Work performed —
Added `app.telegram_activation` for preflight, arm/disarm, outbox delivery, polling and webhook intake, recipient binding checks, send ledger, inbound cursor, health posture, and a rollback checklist. Extended the security protocol so retry waits are computed from durable `attempt` and `updated_at`, and `RATE_LIMITED` does not increment the attempt. Alembic `e0f1a2b3c4d5` adds `telegram_activation_inbound_cursors` and `telegram_activation_send_ledger`. Staging and production deployment safety reject the new arming flags. `render.yaml` was not changed. `create_app` does not mount the webhook. No live Telegram message was sent. Nothing was deployed or armed.

## 4. Files created, changed, or removed —
See commit `4f244d6`. New package `backend/src/app/telegram_activation/`, migration `e0f1a2b3c4d5`, scripts under `scripts/telegram-paper-activation-*.sh`, and `docs/telegram_paper_activation.md`.

## 5. Commands and tests run —
- `uv run ruff check .` and `uv run ruff format --check .`
- `uv run mypy --strict` on the activation modules and related stores
- `uv run pytest -q --tb=line` (full backend, PostgreSQL 16 local)
- Re-run of the two route allowlist tests plus activation, activation-postgres, and Alembic head tests
- Frontend `npm run lint`, `typecheck`, `test`, `build`
- `uv run python ../evaluation/evaluate_agent.py`, `evaluate_rag.py`, `evaluate_guardrails.py`
- `python -m app.telegram_activation preflight --expect-disabled` and `rollback` / `rollback --apply`
- Docker image build was not run (no Docker daemon)

## 6. Exact results —
Ruff passed. mypy --strict passed on the activation modules (pre-existing untyped helpers in `main.py` were not part of this change). Full pytest failed 2 tests, both because `GET /health/telegram-paper-activation` was not in the known non-inbound route set. After the allowlist update, those two tests and the activation/Alembic tests passed (20). Frontend: lint clean, typecheck clean, 1195 tests passed, build succeeded. Evaluation 16/16, RAG 5/5, guardrails 7/7. Preflight exit 0 with verdict `NOT_ARMED`. Rollback exit 0. Rollback `--apply` exit 2. GitHub CI run 35742331530 was in progress at snapshot time.

## 7. Latest successful step —
Pushed `4f244d6` and opened draft PR #129. Local evidence supports READY FOR CONTROLLED ACTIVATION. The process remains disarmed.

## 8. Blockers or review requests —
None. CI confirmation is still outstanding. No human approval is required to keep the process disarmed.

## 9. Warnings and risks —
Do not set `TELEGRAM_PAPER_ACTIVATION_ARMED`, `TELEGRAM_NETWORK_PERMITTED`, or a non-off inbound mode in staging or production. Do not install `paper_scan_hook()` on the Watcher worker as part of this task. The in-memory outbound window resets on process restart; the durable `RATE_LIMITED` wait remains on the outbox row.

## 10. Next recommended step —
Wait for CI run 35742331530. If green, mark the handoff READY and leave activation off. If red, fix the failed check without arming or deploying.
