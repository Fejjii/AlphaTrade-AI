<!-- METADATA
Project: AlphaTrade AI
Document Type: CHANGELOG_SESSION
Schema Version: 2.0
Generated At Local: 2026-09-24T13:45:07+00:00
Generated At UTC: 2026-09-24T13:45:07Z
Timezone: UTC
Session ID: AT-SESSION-20260924-123856
Task ID: AT-085
Current Branch: cursor/binance-418-recovery-11ce
Current Commit: 75a5273
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: IN_PROGRESS
Source File SHA256: 3e361855acb201a0793fab2ba3484ed493e501f98e98d1902ca697a7c6e49fa4
END METADATA -->

# AlphaTrade AI — Session Changelog (latest session only)

## 1. Session metadata —
AT-SESSION-20260924-123856. Task AT-085. Host timezone UTC. Status IN_PROGRESS.

## 2. Starting state —
Product commit `f883f3b` was on draft PR #138.

## 3. Work performed —
Exact-head CI run 36003884906 failed `test_disconnect_reconnect_recovers_without_reusing_connection_identity`. The in-backoff tick after a regional failure was `unavailable`. `75a5273` keeps that tick `degraded` and keeps the reason `PROVIDER_UNAVAILABLE`, so Watcher still reports `provider_outage`. A failed recovery attempt stays unavailable and does not publish a price.

## 4. Files created, changed, or removed —
`backend/src/app/market_monitor/runtime.py`.

## 5. Commands and tests run —
`gh run view 36003884906 --log-failed`. `uv run ruff check` and `uv run ruff format --check` on `runtime.py`. `uv run pytest` on the reconnect test, the two Watcher outage tests, and `tests/test_binance_usdm_staging_reliability.py`.

## 6. Exact results —
CI run 36003884906: backend failure, 1 failed. Local pytest after the fix exit 0. Ruff exit 0. Exact-head CI for `75a5273` is not recorded.

## 7. Latest successful step —
Pushed `75a5273` to `cursor/binance-418-recovery-11ce`.

## 8. Blockers or review requests —
None. Do not merge, deploy, or activate.

## 9. Warnings and risks —
Full backend pytest was not rerun locally. CI is the remaining check.

## 10. Follow-up actions —
Wait for exact-head CI on `75a5273`. Then READY FOR STAGING REVALIDATION or NOT READY.

## 11. Final status —
IN_PROGRESS
