<!-- METADATA
Project: AlphaTrade AI
Document Type: CHANGELOG_SESSION
Schema Version: 2.0
Generated At Local: 2026-09-24T12:38:56+00:00
Generated At UTC: 2026-09-24T12:38:56Z
Timezone: UTC
Session ID: AT-SESSION-20260924-123856
Task ID: AT-085
Current Branch: cursor/binance-418-recovery-11ce
Current Commit: 21c020f
Working Tree Status: CLEAN
Generated By: Cursor
Handoff Status: IN_PROGRESS
Source File SHA256: 6d9c5aa6a67def489989f63f95a6f70813be3f2f49dc3629a11f45188c7e72f0
END METADATA -->

# AlphaTrade AI — Session Changelog (latest session only)

## 1. Session metadata —
AT-SESSION-20260924-123856. Task AT-085. Host timezone UTC. Status IN_PROGRESS because exact-head CI is not recorded.

## 2. Starting state —
`main` at `697cc13`. HTTP 418 was in `REGIONAL_STATUS_CODES` and raised `RegionalProviderFailureError` with no retry. `recover_from_backfill` set `GapState.UNRECOVERABLE`, and `SymbolMonitorRuntime` then refused further fetches until process restart.

## 3. Work performed —
Mapped HTTP 418 to `UpstreamBanError`. Retries stay inside the existing attempt budget and only when Retry-After fits `max_backoff_seconds`. The monitor honors a ban Retry-After up to 3 days and does not open a new connection epoch. Empty or incomplete backfill keeps cursor id and connection identity, suppresses a usable price, and can succeed on a later backoff-gated attempt. 401, 403, and 451 are unchanged. 429 stays `RateLimitedError`. No spot or fabricated fallback. ADR AT-ADR-066. Draft PR #138.

## 4. Files created, changed, or removed —
Created `backend/tests/test_binance_usdm_staging_reliability.py`. Edited the HTTP client, Binance status mapping, cursor recovery, monitor runtime and backoff, frontier and phase-5 tests, `docs/market_source_contracts.md`, `.ai/DECISIONS.md`, and `.ai/TASKS.md`.

## 5. Commands and tests run —
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv run mypy --strict` on the six edited modules
- `uv run pytest` on the reliability, phase-5 trade stream, frontier, live monitor, final activation, and phase-5 adapter tests
- `uv run pytest tests/test_deployment_safety.py tests/test_deployment_scripts.py tests/test_config.py tests/test_watcher_paper_activation.py -q`

## 6. Exact results —
Ruff check exit 0. Ruff format exit 0 (920 files already formatted). Mypy exit 0. Targeted pytest exit 0. Deployment-safety pytest exit 0. Full backend pytest was not run in this environment. GitHub Actions exact-head run is not yet recorded.

## 7. Latest successful step —
Pushed `21c020f` and opened draft PR https://github.com/Fejjii/AlphaTrade-AI/pull/138.

## 8. Blockers or review requests —
None. Do not merge, deploy, or activate from this draft.

## 9. Warnings and risks —
A ban longer than the HTTP backoff cap is not retried inside the client; the monitor waits. Coverage that is still incomplete does not produce a usable price. Staging behavior of Frankfurt 418 is not revalidated by this change.

## 10. Follow-up actions —
Record exact-head CI for `21c020f`. Then set READY FOR STAGING REVALIDATION or NOT READY. No deploy. No activation.

## 11. Final status —
IN_PROGRESS
