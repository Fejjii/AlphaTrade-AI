<!-- METADATA
Project: AlphaTrade AI
Document Type: CHANGELOG_SESSION
Schema Version: 2.0
Generated At Local: 2026-08-06T13:18:00+00:00
Generated At UTC: 2026-08-06T13:18:00Z
Timezone: Etc/UTC
Session ID: AT-SESSION-20260806-131800
Task ID: P0-PAPER-CLOSE-EXIT-PRICE-HONESTY
Current Branch: fix/paper_close_exit_price_honesty
Current Commit: pending-handoff-commit
Working Tree Status: DIRTY-UNTIL-COMMIT
Generated By: Cursor
Handoff Status: REVIEW_REQUIRED
Source File SHA256: f6c3eadf5df3f54dbbc5e6dee7a1c12258c7a217be1957a10989bb21762b53d6
END METADATA -->

# AlphaTrade AI — Session Changelog (latest session only)

## 1. Session metadata —
P0 defect correction session on cloud agent. Branch `fix/paper_close_exit_price_honesty`
created from `origin/main@aaf38f566d9a61dd8b2a85686461ec0a58f165de` after verifying the
approved base SHA matched exactly.

## 2. Starting state —
Clean checkout of main at aaf38f5. Staging validation handoff
(`cursor/staging-ops-handoff-a569` @ `b22dd1d`) recorded the P0: explicit paper close submitted
`91234.56` but audit event `4442c6ac-9fac-4e5f-ae0a-8f66697bbc31` recorded
`exit_price=64524.01000000`, `requested_exit_price=91234.56`, `realized_pnl=0.07955`.

## 3. Work performed —
- Traced the full paper close path: `ClosePaperPositionRequest` (schema, requires positive
  decimal `exit_price`) → `POST /positions/{id}/close-paper` route → `PositionService.close_paper`
  → `_resolve_close_exit_price` → position persistence (`realized_pnl`, Numeric(20,8)) → audit
  event (`redacted_metadata`) → AT-033 opt-in journal hook.
- Reproduced the defect locally with a non-mock provider configuration (provider_mode=fallback,
  market_data_provider=binance, fresh ticker at 64524.01): audit recorded exit_price=64524.01
  and realized_pnl=0.07955 for submitted 91234.56 — exact staging values. Root cause proven:
  `_resolve_close_exit_price` (introduced in AT-012 commit `7ebe3b0`) returned
  `ticker.last_price` for every non-mock close, discarding the explicit requested price.
- Fixed `PositionService`: explicit submitted prices are authoritative (validated fail-closed:
  positive, finite Decimal), market data is not consulted for explicit closes; `requested=None`
  (future system closes) keeps fresh-ticker binding and fails closed on missing/stale/degraded
  data; audit event gains `exit_price_source=user_submitted`.
- Fixed `app/core/errors.py` validation handler to encode pydantic errors with
  `jsonable_encoder` (invalid Decimal input previously produced a 500 instead of 422).
- Added `backend/tests/test_paper_close_exit_price_honesty.py` (21 tests) and updated the
  AT-012 close-binding test to the new contract.
- Journal linkage investigation: AT-033 auto-journal on close is opt-in
  (`journal_auto_from_position_close=false` by default; staging render.yaml does not set it);
  empty staging journal linkage is intended behaviour, not a defect. Documented as follow-up.
- Portfolio staging e2e investigation: FP2-123 intentionally suppresses the redundant safety
  banner for verified standard paper posture (codified in unit tests); updated only the stale
  e2e expectation in `frontend/e2e/portfolio-staging.spec.ts` to branch on the actual safety
  payload and assert the retained StatusStrip/PaperModeIndicator surfaces.

## 4. Files created, changed, or removed —
- M `backend/src/app/services/position_service.py`
- M `backend/src/app/core/errors.py`
- A `backend/tests/test_paper_close_exit_price_honesty.py`
- M `backend/tests/test_at012_paper_risk_at_execution.py`
- M `frontend/e2e/portfolio-staging.spec.ts`
- A/M `HANDOFF.md`, `CHANGELOG_SESSION.md` (committed on explicit task instruction)

## 5. Commands and tests run —
- `uv run pytest tests/test_paper_close_exit_price_honesty.py tests/test_at012_paper_risk_at_execution.py`
- `uv run pytest -q` (full backend)
- `uv run ruff check src tests`; `uv run ruff format --check src tests`; `uv run mypy src`
- `uv run pytest tests/test_deployment_safety.py tests/test_deployment_scripts.py tests/test_config.py`
- `scripts/post-deploy-smoke-gate.sh --self-check`
- `uv run python ../evaluation/evaluate_agent.py|evaluate_rag.py|evaluate_guardrails.py`
- `npm run lint`; `npm run typecheck`; `npm run test`; `npm run build`; `npm run test:e2e` (chromium)

## 6. Exact results —
- Targeted: 38 passed, exit 0.
- Full backend: 1394 passed, 11 skipped, exit 0.
- Ruff check/format: pass. mypy: 375 pre-existing repo-wide errors, none introduced in changed files.
- Deployment safety: 48 passed; smoke-gate self-check pass.
- Evaluation: agent 16/16, RAG 5/5, guardrails 7/7.
- Frontend: lint pass; typecheck pass; 1137 tests passed (184 files); build pass.
- E2E chromium smoke: 20 passed, 13 skipped (staging-gated).
- Docker build job: not runnable locally (no docker binary); delegated to GitHub CI.

## 7. Latest successful step —
Full validation matrix green; handoff regenerated; draft PR being opened on the pushed head.

## 8. Blockers or review requests —
REVIEW_REQUIRED: human review/merge of the draft PR, then staging redeploy and re-run of the
staging exit-price/journal/portfolio gates. No technical blockers.

## 9. Warnings and risks —
- Contract change: audit `exit_price` now always equals the explicit submitted price for user
  closes; the AT-012 server-ticker binding applies only to closes without an explicit price.
- The iCloud handoff sync script (`~/.local/bin/sync-alphatrade-ai-handoff.sh`) is not present
  in this cloud VM (machine-local tooling); sync was skipped and must run on the host machine.

## 10. Follow-up actions —
- Decide whether staging enables `JOURNAL_AUTO_FROM_POSITION_CLOSE=true` (AT-033).
- Staging re-validation after merge + redeploy (exit-price drill, kill switch, portfolio gate).
- Pre-existing mypy debt untouched.

## 11. Final status —
REVIEW_REQUIRED — implementation complete, all local gates green, awaiting human PR review.
